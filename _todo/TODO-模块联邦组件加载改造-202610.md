# 模块联邦组件加载改造 - 202610

> 本文档规划如何利用项目已有的 `wujie-vue2` 和 `hel-micro`，将大型前端依赖通过模块联邦远程加载，减少主应用打包体积。`wujie-vue2` 作为壳应用承载层，`hel-micro` 负责按需加载重型模块（monaco-editor / echarts / mermaid / vega / reveal.js 等）。不支持 Gitee Pages（已不可用），不将组件库（element-ui / antd）CDN 化，组件库保持本地打包。异步加载期间展示定制齿轮加载动画，保证不影响原有功能。

---

## 一、现状分析

### 1.1 现有微前端基础设施

| 组件 | 状态 | 用途 |
|------|------|------|
| `wujie-vue2` | **已使用** | `ImDrawer.vue` 承载 box-im 子应用（远程地址 `https://luo0412.github.io/box-im/#/`） |
| `hel-micro` | **已安装，未使用** | `package.json` devDependencies 中有 `^4.15.3`，代码中无任何引用 |

### 1.2 两种方案的分工

```
+------------------------------------------------------------------+
|                        主应用 (Memocast)                          |
|                                                                   |
|  +-----------------+    +------------------------------------+   |
|  | wujie-vue2      |    |  hel-micro 按需加载重型模块          |   |
|  | (壳应用层)       |    |                                     |   |
|  |                  |    |  monaco-editor (4-6 MB)             |   |
|  | 用于承载          |    |  echarts (2-3 MB)                  |   |
|  | 整个子应用        |    |  mermaid (1-1.5 MB)               |   |
|  |                  |    |  vega+embed (1-1.5 MB)            |   |
|  | 例如:             |    |  reveal.js (1-2 MB)               |   |
|  | - box-im         |    |  markmap (300-500 KB)             |   |
|  | - 设置面板        |    |  flowchart.js (200-300 KB)        |   |
|  | - 博客部署        |    |  jsxgraph (300 KB)                |   |
|  +-----------------+    +------------------------------------+   |
|                                                                   |
|  [本地打包，不走远程]                                               |
|  element-ui + vue-element-ui-x + antd + katex + moment           |
+------------------------------------------------------------------+
```

**分工说明：**
- **wujie-vue2**：用于将一个完整的子应用（包含多个页面、功能模块）作为独立应用加载进来，适合需要完整隔离的复杂功能（如 box-im IM 系统、设置面板、博客部署 UI）
- **hel-micro**：用于按需加载单一重型库（monaco / echarts / mermaid 等），适合将大体积库从主 bundle 剥离，支持预加载和版本管理

### 1.3 当前打包体积瓶颈

项目使用 Quasar CLI（底层 Webpack 4），所有大型依赖均打包进主 bundle。

主要体积来源（按估算体积排序）：

| 依赖 | 估算体积 | 加载场景 | hel-micro 可行性 | 备注 |
|------|----------|----------|-----------------|------|
| `monaco-editor` | ~4-6 MB | 需用户主动打开 Monaco 编辑器才使用 | **推荐** | 支持 ESM + worker 机制，hel-micro 可托管 |
| `echarts` | ~2-3 MB | 日历视图统计图表 | **推荐** | 全局 UMD，hel-micro 方案成熟 |
| `reveal.js` | ~1-2 MB | PPT 预览时才需要 | **推荐** | ESM 模块化，已有动态 import() |
| `mermaid` | ~1-1.5 MB | Markdown 中嵌入图表 | **推荐** | 已有动态 import()，hel-micro 适配 |
| `vega` + `vega-lite` + `vega-embed` | ~1-1.5 MB | Markdown Vega 图表 | **推荐** | 已有动态 import() |
| `markmap-lib` + `markmap-view` | ~300-500 KB | Markmap 思维导图 | **推荐** | 已有动态 import() |
| `flowchart.js` | ~200-300 KB | Markdown 流程图 | **可做** | 已有动态 import() |
| `jsxgraph` | ~300-500 KB | 数学图表 | **可做** | UMD 全局库 |
| `shiki` | ~2-4 MB | 代码高亮 | **可做** | ESM 模块化 |

**保持本地打包（不远程化）：**

| 依赖 | 原因 |
|------|------|
| `element-ui` | 通过 boot 全量注册，Vue.use() 后全局可用 |
| `vue-element-ui-x` | AI 抽屉核心组件，必须本地 |
| `antd` (ant-design-vue) | 通过 boot 全量注册，Vue.use() |
| `katex` | Muya 内核深度集成，静态 import 分散在多个文件 |
| `moment` | 被 antd 依赖 |
| `wujie-vue2` | 框架层，必须本地 |
| `hel-micro` | 框架层，必须本地 |
| `quasar` | 核心框架，必须本地 |

### 1.4 现有动态加载模式

项目已有良好的动态 `import()` 用法：

- `src/libs/muya/lib/renderers/index.js` - `mermaid` / `flowchart` / `vega-lite` 通过 `import()` 懒加载
- `src/libs/muya/lib/parser/render/index.js` - 图表渲染器按需加载
- `src/libs/muya/lib/utils/exportHtml.js` - 导出时按需加载图表库
- `src/components/ui/editor/Monaco.vue` - Monaco 编辑器组件（静态 import）
- `src/components/ui/editor/MarkMap.vue` - Markmap（静态 import）
- `src/components/ui/dialog/RuneFormDialog.vue` - Monaco（静态 import）
- `src/components/ui/dialog/EchoFormDialog.vue` - Monaco（静态 import）
- `src/components/ui/dialog/PptPreviewDialog.vue` - Reveal.js（静态 import）
- `src/components/CategoryTreePanel.vue` - echarts（静态 import）

### 1.5 现有加载动画

项目已有多种加载动画：

1. **双球弹跳动画**（`src/components/ui/Loading.vue` + `src/index.template.html`）：双球弹跳 CSS 动画，颜色跟随 `--themeColor` 主题变量
2. **Quasar QSpinnerGears**（`src/store/server/actions.js`）：用于导出操作时的齿轮加载
3. **Sync 旋转图标**（`src/components/Header.vue`）：同步图标旋转动画

---

## 二、技术方案

### 2.1 核心架构

```
RemoteModuleLoader (Service)
  +-- HelMicroAdapter       +-- hel-micro 封装，preFetch / loadModule
  +-- ModuleRegistry        +-- 模块名 -> 版本/路径映射注册表
  +-- UMDLoader            +-- 加载 UMD 全局库（echarts / jsxgraph）
  +-- ESMProxyLoader       +-- 包装 hel-micro ESM 模块，返回与原 import 等价的接口
  +-- LoadingOverlay       +-- 齿轮加载动画管理（展示/隐藏）
```

**两种加载策略并存：**
- **wujie-vue2**：加载独立完整子应用（box-im / 设置面板 / 博客部署等）
- **hel-micro**：按需加载单一重型库（monaco / echarts / mermaid 等），注入到主应用 DOM 树

### 2.2 hel-micro 接入方案

hel-micro v4 支持 Vue 2，通过 `preFetchApp` 和 `loadSecondaryApp` 机制托管模块：

```javascript
import { HelMicro, HelMetaDict } from 'hel-micro'

const hel = HelMicro.init({
  appName: 'memocast-remote',
  getLocalEntirePath: (appName) => `https://cdn.example.com/${appName}`, // 指向自建静态服务器
  preFetchApps: ['monaco', 'echarts', 'mermaid', 'vega-embed', 'reveal', 'markmap'],
  onBeforeCaptureModule: (module, originPkg) => {
    // 统一处理版本和入口
  }
})
```

**构建端**：需要为每个目标模块单独构建出 hel-micro 可托管的产物，部署到静态服务器（GitHub Releases + jsdelivr CDN，或自建 Nginx）。

**运行时**：hel-micro 优先从本地缓存读取，未命中时从 CDN 下载并缓存。

### 2.3 wujie-vue2 扩展方案

当前 `ImDrawer.vue` 已使用 wujie-vue2 承载 box-im，可扩展为通用子应用加载器：

```javascript
// src/services/WujieLoader.js
export function loadSubApp (name, url, props = {}) {
  // 使用 WujieVue 加载完整子应用
  // 支持预加载（prefetch）
  // 支持生命周期钩子（mount/unmount）
}
```

### 2.4 不影响原有功能的保障

- **返回值兼容**：hel-micro 加载后的模块对象结构与原 `import` 结果完全一致
- **ESM 代理层**：`getESMModule(name)` 替换原 `import()` 调用，返回相同格式对象
- **UMD 全局兼容**：对于 echarts 等全局库，加载后直接使用挂载到 window 的全局名
- **失败回退**：加载失败时自动回退本地 `import()`（需保留本地 node_modules）
- **加载锁**：同一模块并发请求只触发一次加载，防止重复注入

---

## 三、可应用范围分析

### 3.1 推荐优先改造（收益高、风险低）

| 模块 | 当前加载方式 | 改造方式 | 预计节省体积 | 风险 |
|------|-------------|----------|------------|------|
| `mermaid` | 动态 import() in muya | hel-micro ESM 托管 | ~1-1.5 MB | 低 |
| `vega` + `vega-lite` + `vega-embed` | 动态 import() in muya | hel-micro ESM 托管 | ~1-1.5 MB | 低 |
| `flowchart.js` | 动态 import() in muya | hel-micro ESM 托管 | ~200-300 KB | 低 |
| `echarts` | 静态 import，首页即打包 | hel-micro UMD 托管 | ~2-3 MB | 低 |
| `reveal.js` | 静态 import（PPT 预览） | hel-micro ESM 托管 | ~1-2 MB | 低 |
| `markmap-lib` + `markmap-view` | 静态 import（Markmap 编辑器） | hel-micro ESM 托管 | ~300-500 KB | 低 |

### 3.2 中等优先级改造

| 模块 | 当前加载方式 | 改造方式 | 预计节省体积 | 风险 | 额外工作 |
|------|-------------|----------|------------|------|----------|
| `monaco-editor` | 静态 import，多处使用 | hel-micro ESM + worker 适配 | ~4-6 MB | 中 | Monaco worker 文件需单独处理 CDN 路径配置 |
| `jsxgraph` | 静态 import | hel-micro UMD 托管 | ~300 KB | 低 | 需处理 CSS |
| `shiki` | 静态 import | hel-micro ESM 托管 | ~2-4 MB | 低 | 需确认 ESM 入口 |

### 3.3 wujie-vue2 扩展（子应用隔离）

| 子应用 | 当前方式 | 改造方式 | 收益 |
|--------|---------|----------|------|
| box-im | 已有，wujie-vue2 | 已有，继续使用 | - |
| 设置面板 | 内嵌本地 | 封装为 wujie 子应用，独立部署 | 解耦 + 可独立迭代 |
| 博客部署表单 | 本地组件 | 封装为 wujie 子应用，独立部署 | 解耦 + 可独立迭代 |
| AI 助手面板 | 本地组件 | 封装为 wujie 子应用 | 解耦 + 可独立迭代 |

---

## 四、改造步骤

### 阶段一：基础设施搭建

#### 步骤 1.1：创建 hel-micro 模块注册配置

**新建文件：** `src/services/remoteModules.js`

定义每个远程模块的元信息（版本、入口文件、加载方式）：

```javascript
// src/services/remoteModules.js
export const REMOTE_MODULES = {
  monaco: {
    version: '0.38.0',
    mainFile: 'min/vs/loader.js',
    type: 'esm',
    deps: ['min/vs/editor/editor.worker.js', 'min/vs/language/json/json.worker.js'],
    appName: 'monaco-editor'
  },
  echarts: {
    version: '5.4.3',
    mainFile: 'dist/echarts.min.js',
    type: 'umd',
    globalName: 'echarts',
    appName: 'echarts'
  },
  mermaid: {
    version: '8.13.8',
    mainFile: 'dist/mermaid.esm.min.mjs',
    type: 'esm',
    appName: 'mermaid'
  },
  'vega-embed': {
    version: '6.4.0',
    mainFile: 'build/vega-embed.module.min.js',
    type: 'esm',
    deps: ['vega', 'vega-lite'],
    appName: 'vega-embed'
  },
  vega: {
    version: '5.10.0',
    mainFile: 'dist/vega.min.js',
    type: 'umd',
    globalName: 'vega',
    appName: 'vega'
  },
  'vega-lite': {
    version: '4.7.0',
    mainFile: 'dist/vega-lite.min.js',
    type: 'umd',
    globalName: 'vl',
    appName: 'vega-lite'
  },
  flowchart: {
    version: '1.13.0',
    mainFile: 'dist/flowchart.min.js',
    type: 'umd',
    globalName: 'flowchart',
    appName: 'flowchart.js'
  },
  reveal: {
    version: '4.6.1',
    mainFile: 'js/reveal.esm.js',
    type: 'esm',
    appName: 'reveal.js'
  },
  'markmap-lib': {
    version: '0.11.6',
    mainFile: 'dist/index.esm.js',
    type: 'esm',
    appName: 'markmap-lib'
  },
  'markmap-view': {
    version: '0.2.6',
    mainFile: 'dist/index.esm.js',
    type: 'esm',
    deps: ['markmap-lib'],
    appName: 'markmap-view'
  },
  jsxgraph: {
    version: '1.10.1',
    mainFile: 'distrib/jsxgraphcore.js',
    type: 'umd',
    globalName: 'JXG',
    appName: 'jsxgraph'
  }
}
```

#### 步骤 1.2：创建 RemoteModuleLoader 服务

**新建文件：** `src/services/RemoteModuleLoader.js`

关键实现：
```javascript
// src/services/RemoteModuleLoader.js
import { HelMicro, HelMetaDict } from 'hel-micro'
import { REMOTE_MODULES } from './remoteModules'

let staticBaseUrl = '/remote-modules'
const moduleCache = new Map()      // moduleName -> module
const loadingPromises = new Map()  // moduleName -> Promise（防并发重复加载）
const scriptTags = new Map()       // url -> <script> element
let helInstance = null
let loadingCtrl = null             // 外部挂载的加载动画控制

function showLoading (tip) {
  if (loadingCtrl) loadingCtrl.show(tip || '正在加载模块...')
}
function hideLoading () {
  if (loadingCtrl) loadingCtrl.hide()
}

export const RemoteModuleLoader = {
  /** 挂载加载动画控制引用（由 main.js 注入） */
  mountLoadingControl (ctrl) {
    loadingCtrl = ctrl
  },

  /** 初始化 hel-micro 并设置静态资源基础路径 */
  init (staticBase) {
    staticBaseUrl = staticBase || staticBaseUrl
    helInstance = HelMicro.init({
      appName: 'memocast',
      getLocalEntirePath: (appName) => `${staticBaseUrl}/${appName}/index.json`,
    })
    window.__REMOTE_MODULE_LOADER__ = this
  },

  getStaticBase () { return staticBaseUrl },

  setStaticBase (url) { staticBaseUrl = url },

  /** 统一加载入口 */
  async get (moduleName) {
    // 1. 命中缓存
    if (moduleCache.has(moduleName)) return moduleCache.get(moduleName)

    // 2. 防并发重复加载
    if (loadingPromises.has(moduleName)) return loadingPromises.get(moduleName)

    // 3. hel-micro 已预加载
    const config = REMOTE_MODULES[moduleName]
    if (!config) throw new Error(`[RemoteModuleLoader] 未注册模块: ${moduleName}`)
    const helMeta = HelMetaDict[config.appName]
    if (helMeta && helMeta.module) {
      moduleCache.set(moduleName, helMeta.module)
      return helMeta.module
    }

    // 4. 发起加载
    showLoading(`正在加载 ${moduleName}...`)
    const promise = this._load(moduleName, config)
    loadingPromises.set(moduleName, promise)
    try {
      const mod = await promise
      moduleCache.set(moduleName, mod)
      return mod
    } catch (err) {
      console.warn(`[RemoteModuleLoader] ${moduleName} 远程加载失败，尝试 fallback`, err)
      return this.fallback(moduleName)
    } finally {
      hideLoading()
      loadingPromises.delete(moduleName)
    }
  },

  async _load (moduleName, config) {
    config = config || REMOTE_MODULES[moduleName]
    return config.type === 'umd'
      ? this._loadUMD(moduleName, config)
      : this._loadESM(moduleName, config)
  },

  /** 加载 UMD 全局库（echarts / flowchart / vega / jsxgraph） */
  async _loadUMD (moduleName, config) {
    const url = `${staticBaseUrl}/${config.appName}/${config.mainFile}`
    if (scriptTags.has(url) && window[config.globalName]) {
      return window[config.globalName]
    }
    await this.loadScript(url, `rml-${moduleName}`)
    if (!window[config.globalName]) {
      throw new Error(`[RemoteModuleLoader] UMD 模块 ${moduleName} 加载但 window.${config.globalName} 不存在`)
    }
    return window[config.globalName]
  },

  /** 加载 ESM 模块（hel-micro -> dynamic import fallback） */
  async _loadESM (moduleName, config) {
    const helMeta = HelMetaDict[config.appName]
    if (helMeta && helMeta.module) return helMeta.module
    try {
      return await HelMicro.loadSecondaryApp(config.appName)
    } catch {
      const url = `${staticBaseUrl}/${config.appName}/${config.mainFile}`
      return import(/* @dynamic-import-source: ${moduleName} */ url)
    }
  },

  /** 动态注入 <script>，幂等 + Promise 缓存 */
  loadScript (url, id) {
    if (scriptTags.has(url)) return scriptTags.get(url)
    const promise = new Promise((resolve, reject) => {
      const existing = document.querySelector(`script[data-rml-id="${id}"]`)
      if (existing) { existing.addEventListener('load', resolve); existing.addEventListener('error', reject); return }
      const script = document.createElement('script')
      script.src = url; script.setAttribute('data-rml-id', id); script.async = true
      script.onload = () => resolve(script)
      script.onerror = () => reject(new Error(`[RemoteModuleLoader] 脚本加载失败: ${url}`))
      document.head.appendChild(script)
    })
    scriptTags.set(url, promise)
    return promise
  },

  /** 加载失败时回退本地 node_modules import */
  async fallback (moduleName) {
    console.info(`[RemoteModuleLoader] ${moduleName} 回退到本地 import`)
    const fallbacks = {
      echarts:       () => import('echarts'),
      mermaid:       () => import('mermaid/dist/mermaid.esm.min.mjs'),
      'vega-embed':  () => import('vega-embed'),
      vega:          () => import('vega'),
      'vega-lite':   () => import('vega-lite'),
      flowchart:     () => import('flowchart.js'),
      reveal:        () => import('reveal.js'),
      'markmap-lib': () => import('markmap-lib'),
      'markmap-view':() => import('markmap-view'),
      jsxgraph:      () => import('jsxgraph/distrib/jsxgraphcore'),
      monaco:        () => import('monaco-editor'),
    }
    const fn = fallbacks[moduleName]
    if (!fn) throw new Error(`[RemoteModuleLoader] ${moduleName} 没有 fallback 函数`)
    const mod = await fn()
    moduleCache.set(moduleName, mod)
    return mod
  },

  /** 预加载一组模块（应用空闲时调用，不阻塞首屏） */
  async preFetch (moduleNames) {
    const promises = moduleNames
      .filter(name => !moduleCache.has(name))
      .map(name => {
        const config = REMOTE_MODULES[name]
        if (!config) return Promise.resolve()
        return HelMicro.preFetchApp(config.appName).catch(err => {
          console.warn(`[RemoteModuleLoader] preFetch ${name} 失败:`, err)
        })
      })
    await Promise.allSettled(promises)
  },

  getCached (moduleName) { return moduleCache.get(moduleName) || null },
  clearCache () { moduleCache.clear() }
}
```

**electron-store 配置持久化（可选）：**

```javascript
// 将 CDN 地址持久化到 electron-store
import Store from 'electron-store'
const store = new Store({ name: 'memocast-config' })

// 读取
const base = store.get('remoteModulesBase', '/remote-modules')

// 写入（运行时切换）
RemoteModuleLoader.setStaticBase(newUrl)
store.set('remoteModulesBase', newUrl)
```

---

#### 步骤 1.3：抽取齿轮加载动画为可复用组件

**新建文件：** `src/components/ui/AsyncLoading.vue`

三组 SVG 齿轮以不同速度、方向旋转，颜色跟随 `--themeColor`：

```vue
<template>
  <transition name="async-loading-fade">
    <div v-if="visible" class="async-loading-overlay">
      <div class="async-loading-backdrop" />
      <div class="async-loading-content">
        <div class="gear-spinner" :style="{ '--spinner-color': color }">
          <!-- 齿轮1：最大，正向旋转 -->
          <svg class="gear gear-1" viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg">
            <g fill="var(--spinner-color)" opacity="0.9">
              <circle cx="50" cy="50" r="30" />
              <path d="M50 0 L58 15 L42 15 Z" />
              <path d="M50 0 L58 15 L42 15 Z" transform="rotate(45 50 50)" />
              <path d="M50 0 L58 15 L42 15 Z" transform="rotate(90 50 50)" />
              <path d="M50 0 L58 15 L42 15 Z" transform="rotate(135 50 50)" />
              <path d="M50 0 L58 15 L42 15 Z" transform="rotate(180 50 50)" />
              <path d="M50 0 L58 15 L42 15 Z" transform="rotate(225 50 50)" />
              <path d="M50 0 L58 15 L42 15 Z" transform="rotate(270 50 50)" />
              <path d="M50 0 L58 15 L42 15 Z" transform="rotate(315 50 50)" />
              <circle cx="50" cy="50" r="12" fill="white" opacity="0.3" />
            </g>
          </svg>
          <!-- 齿轮2：中等，逆向旋转 -->
          <svg class="gear gear-2" viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg">
            <g fill="var(--spinner-color)" opacity="0.65">
              <circle cx="50" cy="50" r="22" />
              <path d="M50 8 L56 20 L44 20 Z" />
              <path d="M50 8 L56 20 L44 20 Z" transform="rotate(60 50 50)" />
              <path d="M50 8 L56 20 L44 20 Z" transform="rotate(120 50 50)" />
              <path d="M50 8 L56 20 L44 20 Z" transform="rotate(180 50 50)" />
              <path d="M50 8 L56 20 L44 20 Z" transform="rotate(240 50 50)" />
              <path d="M50 8 L56 20 L44 20 Z" transform="rotate(300 50 50)" />
              <circle cx="50" cy="50" r="8" fill="white" opacity="0.3" />
            </g>
          </svg>
          <!-- 齿轮3：最小，正向旋转 -->
          <svg class="gear gear-3" viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg">
            <g fill="var(--spinner-color)" opacity="0.4">
              <circle cx="50" cy="50" r="14" />
              <path d="M50 12 L55 22 L45 22 Z" />
              <path d="M50 12 L55 22 L45 22 Z" transform="rotate(72 50 50)" />
              <path d="M50 12 L55 22 L45 22 Z" transform="rotate(144 50 50)" />
              <path d="M50 12 L55 22 L45 22 Z" transform="rotate(216 50 50)" />
              <path d="M50 12 L55 22 L45 22 Z" transform="rotate(288 50 50)" />
              <circle cx="50" cy="50" r="5" fill="white" opacity="0.3" />
            </g>
          </svg>
        </div>
        <p v-if="tip" class="async-loading-tip">{{ tip }}</p>
      </div>
    </div>
  </transition>
</template>

<script>
export default {
  name: 'AsyncLoading',
  props: {
    visible: { type: Boolean, default: false },
    tip: { type: String, default: '' },
    color: { type: String, default: 'var(--themeColor)' }
  }
}
</script>

<style scoped>
.async-loading-overlay {
  position: fixed;
  inset: 0;
  z-index: 9999;
  display: flex;
  align-items: center;
  justify-content: center;
  pointer-events: none;
}
.async-loading-backdrop {
  position: absolute;
  inset: 0;
  background: rgba(0, 0, 0, 0.25);
  backdrop-filter: blur(2px);
}
.async-loading-content {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 16px;
  background: rgba(255, 255, 255, 0.95);
  padding: 24px 32px;
  border-radius: 12px;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.15);
}
.dark-mode .async-loading-content { background: rgba(30, 30, 30, 0.95); }

.gear-spinner { position: relative; width: 80px; height: 80px; }
.gear { position: absolute; top: 50%; left: 50%; transform-origin: center center; }
.gear-1 { width: 80px; height: 80px; margin: -40px 0 0 -40px; animation: gear-spin 3s linear infinite; }
.gear-2 { width: 52px; height: 52px; margin: -26px 0 0 -26px; animation: gear-spin-reverse 2s linear infinite; }
.gear-3 { width: 34px; height: 34px; margin: -17px 0 0 -17px; animation: gear-spin 1.5s linear infinite; }

@keyframes gear-spin {
  from { transform: rotate(0deg); }
  to   { transform: rotate(360deg); }
}
@keyframes gear-spin-reverse {
  from { transform: rotate(360deg); }
  to   { transform: rotate(0deg); }
}
.async-loading-tip {
  margin: 0; font-size: 13px; color: #666; text-align: center; white-space: nowrap;
}
.dark-mode .async-loading-tip { color: #aaa; }
.async-loading-fade-enter-active,
.async-loading-fade-leave-active { transition: opacity 0.2s ease; }
.async-loading-fade-enter,
.async-loading-fade-leave-to { opacity: 0; }
</style>
```

**全局挂载（main.js 中注入）：**

```javascript
// src/main.js
import Vue from 'vue'
import AsyncLoading from 'components/ui/AsyncLoading'
import { RemoteModuleLoader } from 'src/services/RemoteModuleLoader'

const loadingVm = new Vue(AsyncLoading, { propsData: { visible: false } })
loadingVm.$mount()
document.body.appendChild(loadingVm.$el)

RemoteModuleLoader.mountLoadingControl({
  show: (tip) => { loadingVm.visible = true; if (tip) loadingVm.tip = tip },
  hide: () => { loadingVm.visible = false }
})
```

---

#### 步骤 1.4：创建 wujie 通用子应用加载器并改造 ImDrawer

**新建文件：** `src/services/WujieLoader.js`

将现有 `ImDrawer.vue` 中的 wujie 逻辑提取为通用加载服务，并暴露生命周期钩子：

```javascript
// src/services/WujieLoader.js
import WujieVue from 'wujie-vue2'

const appInstances = new Map()

export const WujieLoader = {
  /**
   * 创建子应用配置（供 WujieVue 组件使用）
   * @param {string} name - 子应用唯一名称
   * @param {string} url - 子应用入口 URL
   * @param {object} props - 传递给子应用的属性
   * @param {object} events - 生命周期钩子 { onMount, onUnmount, onError }
   */
  createSubApp (name, url, props = {}, events = {}) {
    return {
      name,
      url,
      sync: false,
      attrs: { style: 'width: 100%; height: 100%;' },
      props: { data: props.data || {}, ...props },
      beforeMount (app) {
        appInstances.set(name, app)
        events.onMount && events.onMount(app)
      },
      mounted () { events.onMounted && events.onMounted() },
      beforeUnmount (app) {
        events.onUnmount && events.onUnmount(app)
        appInstances.delete(name)
      },
      error (err) {
        console.error(`[WujieLoader] ${name} 错误:`, err)
        events.onError && events.onError(err)
      }
    }
  },

  destroySubApp (name) {
    const app = appInstances.get(name)
    if (app && app.destroy) { app.destroy(); appInstances.delete(name) }
  },

  getApp (name) { return appInstances.get(name) || null }
}

export { WujieVue }
```

**改造 ImDrawer.vue 使用 WujieLoader：**

**改动文件：** `src/components/ui/ImDrawer.vue`

将硬编码的 WujieVue 使用改为通过 `WujieLoader.createSubApp()` 生成配置：

```vue
<template>
  <q-drawer ... >
    <div class="im-drawer-container">
      <div class="im-drawer-header">...</div>
      <div class="im-wujie-wrapper">
        <WujieVue
          v-if="visible"
          v-bind="wujieConfig"
        />
      </div>
    </div>
  </q-drawer>
</template>

<script>
import { WujieVue, WujieLoader } from 'src/services/WujieLoader'

export default {
  name: 'ImDrawer',
  components: { WujieVue },
  computed: {
    wujieConfig () {
      return WujieLoader.createSubApp(
        'box-im',
        this.imUrl,
        { data: {} },
        {
          onMount: (app) => console.log('[ImDrawer] box-im mounted'),
          onUnmount: (app) => console.log('[ImDrawer] box-im unmounted'),
          onError: (err) => {
            console.error('[ImDrawer] box-im error:', err)
            this.$q.notify({ message: 'IM 模块加载失败', type: 'negative' })
          }
        }
      )
    }
  }
}
</script>
```

**改造要点：**
- `imUrl` 计算属性保持不变，仍返回 `'https://luo0412.github.io/box-im/#/'`
- 生命周期钩子通过 `WujieLoader.createSubApp()` 的第三个参数传入，清晰解耦
- 未来新增子应用（设置面板 / 博客部署等）只需调用 `WujieLoader.createSubApp(name, url, props, events)`，无需修改 ImDrawer 组件

### 阶段二：图表类模块 hel-micro 化

#### 步骤 2.1：改造 echarts（CategoryTreePanel.vue）

**改动文件：** `src/components/CategoryTreePanel.vue`

当前静态 import：
```javascript
import * as echarts from 'echarts'
```

改为：
```javascript
import { RemoteModuleLoader } from 'src/services/RemoteModuleLoader'

export default {
  methods: {
    async initChart () {
      const echarts = await RemoteModuleLoader.get('echarts')
      this.chart = echarts.init(dom)
    }
  }
}
```

#### 步骤 2.2：改造 muya 图表渲染器（mermaid / vega / flowchart）

**改动文件：** `src/libs/muya/lib/renderers/index.js`

将硬编码的 `import()` 替换为通过 `RemoteModuleLoader` 加载：

```javascript
// 统一通过 RemoteModuleLoader 加载，自动走 hel-micro
const loader = window.__REMOTE_MODULE_LOADER__
case 'flowchart':
  m = await loader.get('flowchart')
case 'mermaid':
  m = await loader.get('mermaid')
case 'vega-lite':
  m = await loader.get('vega-embed')
```

由于 hel-micro 加载的模块与原 import 结果兼容，改动调用方代码即可，Muya 内核无需修改。

#### 步骤 2.3：改造 muya exportHtml 中的图表加载

**改动文件：** `src/libs/muya/lib/utils/exportHtml.js`

将静态 `import katex from 'katex'` 和动态 `import('mermaid')`、`import('vega-embed')` 等替换为 `RemoteModuleLoader.get()`。

注：`katex` 暂保持本地 import（Muya 内核深度集成），其他图表库均可 hel-micro 化。

### 阶段三：编辑器/预览模块 hel-micro 化

#### 步骤 3.1：改造 Monaco Editor

**改动文件：**
- `src/components/ui/editor/Monaco.vue`
- `src/components/ui/dialog/RuneFormDialog.vue`
- `src/components/ui/dialog/EchoFormDialog.vue`

Monaco 特殊性在于 worker 文件（`editor.worker.js`、`json.worker.js` 等），加载后需配置 `MonacoEnvironment.getWorkerUrl` 指向 hel-micro 托管的 worker 文件：

```javascript
async loadMonaco () {
  const monaco = await RemoteModuleLoader.get('monaco')

  // 配置 worker URL（hel-micro 加载后 worker 资源已在 CDN）
  window.MonacoEnvironment = {
    getWorkerUrl (moduleId, label) {
      const base = RemoteModuleLoader.getStaticBase()
      const workerMap = {
        json: 'min/vs/language/json/json.worker.js',
        css: 'min/vs/language/css/css.worker.js',
        html: 'min/vs/language/html/html.worker.js',
        typescript: 'min/vs/language/typescript/ts.worker.js',
        editor: 'min/vs/editor/editor.worker.js'
      }
      return `${base}/monaco-editor/${workerMap[label] || workerMap.editor}`
    }
  }

  return monaco
}
```

#### 步骤 3.2：改造 Reveal.js（PPT 预览）

**改动文件：** `src/components/ui/dialog/PptPreviewDialog.vue`

```javascript
import { RemoteModuleLoader } from 'src/services/RemoteModuleLoader'

export default {
  methods: {
    async loadReveal () {
      const [Reveal, Markdown, Highlight] = await Promise.all([
        RemoteModuleLoader.get('reveal'),
        RemoteModuleLoader.get('reveal.js/plugin/markdown/markdown'),
        RemoteModuleLoader.get('reveal.js/plugin/highlight/highlight')
      ])
      Reveal.initialize({
        plugins: [Markdown, Highlight]
      })
    }
  }
}
```

#### 步骤 3.3：改造 Markmap

**改动文件：** `src/components/ui/editor/MarkMap.vue`

```javascript
import { RemoteModuleLoader } from 'src/services/RemoteModuleLoader'

export default {
  methods: {
    async loadMarkmap () {
      const [markmapLib, markmapView] = await Promise.all([
        RemoteModuleLoader.get('markmap-lib'),
        RemoteModuleLoader.get('markmap-view')
      ])
      this.transformer = new markmapLib.Transformer()
      this.Markmap = markmapView.Markmap
    }
  }
}
```

### 阶段四：构建端配置（子应用构建）

#### 步骤 4.1：创建 hel-micro 构建脚本

**新建文件：** `scripts/build-remote-modules.js`

为每个目标模块单独构建 hel-micro 产物：

```javascript
// scripts/build-remote-modules.js
// 使用 hel-micro 的 buildRemoteApis 命令
// 构建产物输出到 public/remote-modules/
// 构建后将 public/remote-modules/ 部署到静态服务器
```

**构建产物目录结构：**
```
public/remote-modules/
  +-- echarts/
  |     +-- index.json        (hel-micro 元信息)
  |     +-- dist/echarts.min.js
  +-- mermaid/
  |     +-- index.json
  |     +-- dist/mermaid.esm.min.mjs
  +-- monaco-editor/
        +-- index.json
        +-- min/vs/loader.js
```

构建方式：hel-micro 提供了 `buildRemoteApis` CLI，可自动从 package.json 提取版本和入口，构建出 hel-micro 可托管的产物。

#### 步骤 4.2：配置 webpack externals

**改动文件：** `quasar.conf.js`

将已 hel-micro 化的模块标记为 externals，不再打包进主 bundle：

```javascript
// quasar.conf.js build.extendWebpack
const remoteModules = [
  'echarts', 'mermaid', 'flowchart', 'vega-embed',
  'vega', 'vega-lite', 'reveal', 'markmap-lib',
  'markmap-view', 'jsxgraph'
]

remoteModules.forEach(mod => {
  cfg.externals[mod] = `__remote_module__(${mod})`
})
```

monaco-editor 特殊性最强，worker 文件较多，建议保持 node_modules 中但通过 webpack externals 排除主 bundle，仅在 fallback 时使用。

#### 步骤 4.3：配置 electron-builder 排除已远程化的模块

**改动文件：** `quasar.conf.js`

将已 hel-micro 化的模块添加到 asarUnpack 或从 builder.files 排除：

```javascript
// quasar.conf.js electron.builder.files 中追加
// 已远程化的模块不再打包进安装包（保留 node_modules 作为 fallback）
```

### 阶段五：wujie-vue2 扩展（子应用隔离）

#### 步骤 5.1：将设置面板封装为 wujie 子应用

**新建子应用：** `_plugins/box-settings/`

将 `src/components/ui/dialog/SettingsDialog.vue` 及相关组件整体迁移为独立子应用，通过 wujie-vue2 加载：

```javascript
// src/layouts/MainLayout.vue 中
import { WujieLoader } from 'src/services/WujieLoader'

const settingsConfig = WujieLoader.createSubApp(
  'settings',
  'https://your-cdn.com/settings/',
  { theme: 'dark' }
)
```

#### 步骤 5.2：将博客部署 UI 封装为 wujie 子应用

参考已有：`src/components/ui/dialog/BlogDeployDialog.vue` 及相关组件，可整体迁移为独立子应用。

#### 步骤 5.3：hel-micro 预加载策略

在 `src/boot/` 中初始化预加载：

```javascript
// src/boot/hel-micro.js
import { RemoteModuleLoader } from 'src/services/RemoteModuleLoader'

export default async ({ app, router, store, Vue }) => {
  // 静态资源基础 URL（可配置指向 CDN 或本地 dev 服务器）
  const staticBase = process.env.REMOTE_MODULES_BASE || '/remote-modules'
  await RemoteModuleLoader.init(staticBase)

  // 预加载高频使用的重型模块（应用空闲时预加载，不阻塞首屏）
  if (process.env.MODE !== 'electron') {
    requestIdleCallback(() => {
      RemoteModuleLoader.preFetch(['mermaid', 'vega-embed', 'flowchart', 'echarts'])
    })
  }
}
```

---

## 五、版本与部署

### 5.1 hel-micro 静态资源部署

hel-micro 托管的模块构建产物建议部署到以下位置之一：

| 方案 | 地址 | 优缺点 |
|------|------|--------|
| GitHub Releases + jsdelivr | `https://cdn.jsdelivr.net/gh/{user}/{repo}@{tag}/public/remote-modules/` | 免费、全球 CDN，但需手动发布 Release |
| GitHub Pages | `https://{user}.github.io/{repo}/remote-modules/` | 自动，但国内访问不稳定 |
| 自建 Nginx / OSS | 自定义域名 | 稳定、可控，但需维护服务器 |

### 5.2 离线 fallback 机制

当 CDN 不可达时，自动回退本地 node_modules import：

```javascript
async get (moduleName) {
  try {
    return await this._loadFromHel(moduleName)
  } catch (e) {
    console.warn(`[RemoteModuleLoader] hel-micro 加载 ${moduleName} 失败，回退本地 import`)
    return await import(/* webpackIgnore: true */ `${moduleName}`)
  }
}
```

---

## 六、风险与注意事项

1. **hel-micro Vue 2 兼容性**：hel-micro v4 对 Vue 2 的支持需验证，确保 `preFetchApp` 和 `loadSecondaryApp` 在 Vue 2 环境下正常工作
2. **Monaco worker 文件**：worker 文件需通过 CDN 单独托管，配置 `MonacoEnvironment.getWorkerUrl` 指向 CDN 路径
3. **Muya 内核依赖**：katex 等被 Muya 深度集成的库，暂保持本地 import
4. **构建链路变更**：需要为 hel-micro 单独构建模块产物，CI/CD 流程需增加构建步骤
5. **wujie 子应用隔离**：子应用需要单独构建和部署，增加了维护复杂度，建议选择性使用

---

## 七、改造优先级与工作量估算

| 阶段 | 模块 | 预计工作量 | 预计节省体积 | 优先级 |
|------|------|-----------|-------------|--------|
| 阶段一 | RemoteModuleLoader + AsyncLoading + WujieLoader | 中 | - | P0 |
| 阶段二 | echarts + mermaid/vega/flowchart/markmap/reveal | 中 | ~5 MB | P0 |
| 阶段三 | Monaco Editor + 构建产物 | 大 | ~4-6 MB | P1 |
| 阶段四 | hel-micro 构建脚本 + CI/CD 集成 | 中 | - | P1 |
| 阶段五 | wujie 子应用扩展（设置/博客部署/AI面板） | 大 | 视子应用数量 | P2 |
