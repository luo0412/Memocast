---
name: echo-anno-plan
overview: 将“我的回响”从当前的卡片占位补丁升级为可注册的 anno 规则系统：保留与符文并列的设置入口和 `@` 快捷插入体验，同时让 Muya 原生识别 `@回响名{...}(...)` 并在编辑回响后重新注册生效。
todos:
  - id: upgrade-echo-model
    content: 梳理并升级 echo 数据模型、DB 字段与 store 接口为 anno 规则注册表
    status: pending
  - id: refactor-echo-form
    content: 重构设置页回响表单为 anno_source 编辑并接入保存后重新 register
    status: pending
  - id: hook-echo-quick-insert
    content: 在 Muya quick insert 中将回响与符文并列接入
    status: pending
  - id: add-echo-parser-rule
    content: 在 Muya parser 中新增 `@回响名{...}(...)` inline 规则与 token
    status: pending
  - id: build-echo-runtime
    content: 实现 EchoRegistry / EchoRuntime / inline renderer 渲染链路
    status: pending
  - id: cleanup-and-verify
    content: 清理旧 echo 字符串补丁并做针对性验证
    status: pending
isProject: false
---

# 回响 anno 接入计划

## 目标
- 保留“我的回响”与“我的符文”并列的管理入口和 `@` 快速唤起体验。
- 将回响用法定为 `@回响名{ attr1: 'xxx', attr2: 1 }(ai prompt string)`。
- Monaco 编辑框保存的是 anno 定义源码，参考 [coolma](https://github.com/ui-javascript/coolma) 与 [demo anno 目录](https://github.com/ui-javascript/demo-coolma-202205/tree/master/_coolma/anno) 的组织方式。
- 编辑回响后，像符文一样立即重新 register，到 Muya 的快速插入面板与渲染链路中。

## 现状判断
- 当前 Muya 解析入口在 [src/libs/muya/lib/parser/index.js](src/libs/muya/lib/parser/index.js)，inline 规则在 [src/libs/muya/lib/parser/rules.js](src/libs/muya/lib/parser/rules.js)，inline renderer 注册点在 [src/libs/muya/lib/parser/render/renderInlines/index.js](src/libs/muya/lib/parser/render/renderInlines/index.js)。
- 当前符文/回响管理入口已经在 [src/components/ui/dialog/SettingsDialog.vue](src/components/ui/dialog/SettingsDialog.vue) 搭好，但“回响”仍是卡片模型，不是 anno 规则模型。
- 当前回响编辑器仍复用 [src/components/ui/dialog/RuneFormDialog.vue](src/components/ui/dialog/RuneFormDialog.vue)，Monaco 内容是模板文本，不足以承载 anno 规则定义。
- 当前 `Muya.vue` 中的 echo 逻辑是字符串迁移式补丁，不适合作为正式 parser 方案。

## 设计方案

### 1. 数据模型升级为“回响规则注册表”
在以下文件统一 echo 数据结构：
- [src/store/client/state.js](src/store/client/state.js)
- [src/store/client/actions.js](src/store/client/actions.js)
- [src/utils/DatabaseClient.js](src/utils/DatabaseClient.js)
- [src-electron/main-process/electron-main.js](src-electron/main-process/electron-main.js)

将 `echoes` 表与前端对象升级为：
- `id`
- `name`：对应 `@回响名`
- `desc`
- `icon`
- `color`
- `anno_source`：Monaco 编辑的 anno 源文本
- `render_type`：首版固定 `anno`，保留扩展位
- `created_at` / `updated_at`

保留现有列表、排序、保存 API，但把模板语义切到 `anno_source`。必要时对当前 `template` 做兼容迁移。

### 2. 回响编辑完成后重新 register
在 [src/components/ui/dialog/SettingsDialog.vue](src/components/ui/dialog/SettingsDialog.vue) 的 echo 保存、删除、排序后：
- 更新 `echoCards`
- 触发与符文一致的渲染刷新事件
- 调用 Muya 的回响注册刷新方法（新增，例如 `refreshEchoDefinitions()`）

Muya 侧通过 `options.echoCards` 或专门 registry 挂载最新回响定义，使：
- `@` 面板立即出现最新回响
- 已打开编辑器中的后续渲染立即使用新定义

### 3. `@` 快速插入面板与符文并列
在 [src/components/ui/editor/Muya.vue](src/components/ui/editor/Muya.vue) 与 Muya quick insert provider 中：
- 保留现有 rune 项
- 新增 echo 项
- echo 插入内容不再是占位 `<div>`，而是标准语法骨架，例如：
  - `@回响名{}()`
  - 或带示例 attrs/prompt 的初始文本

首版建议在插入项中：
- title 显示回响名
- subTitle 显示 desc
- onSelect 插入标准注解语法

### 4. Muya parser 原生支持回响语法
在 parser 层接入，而不是继续依赖 `Muya.vue` 的前置正则替换。

目标改动文件：
- [src/libs/muya/lib/parser/rules.js](src/libs/muya/lib/parser/rules.js)
- [src/libs/muya/lib/parser/index.js](src/libs/muya/lib/parser/index.js)
- [src/libs/muya/lib/parser/render/renderInlines/index.js](src/libs/muya/lib/parser/render/renderInlines/index.js)
- 新增 echo inline renderer 文件

新增一个 inline 规则，用于识别：
- `@回响名{ attr1: 'xxx', attr2: 1 }(ai prompt string)`

产出的 token 至少包含：
- `type: 'echo_anno'`
- `echoName`
- `attrsRaw`
- `attrsParsed`
- `prompt`
- `raw`
- `range`

### 5. EchoRuntime：把 token 映射到 anno 定义
新增一个轻量运行时模块，例如：
- `src/components/ui/editor/echo/EchoRegistry.js`
- `src/components/ui/editor/echo/EchoRuntime.js`

职责：
- 根据 `echoName` 查找设置页中保存的回响定义
- 读取 `anno_source`
- 将 `attrs + prompt + echo meta` 注入定义
- 产出首版可渲染结果

首版先做“与符文并列、可见、可用”的最小实现：
- anno 源文本中支持一套有限约定
- 先输出卡片/提示块样式
- 后续再扩成更多 demo anno 风格

### 6. 编辑器封装层只保留注册与刷新，不再主导语法改写
在 [src/components/ui/editor/Muya.vue](src/components/ui/editor/Muya.vue)：
- 删除或降级当前 echo 正则迁移逻辑
- 保留 rune 的兼容迁移逻辑
- 新增 watch / refresh 逻辑：当 `echoCards` 更新时，刷新 echo registry、quick insert、inline render

### 7. 设置页表单语义重构
优先把 [src/components/ui/dialog/RuneFormDialog.vue](src/components/ui/dialog/RuneFormDialog.vue) 拆出专门的 echo 表单，例如：
- [src/components/ui/dialog/EchoFormDialog.vue](src/components/ui/dialog/EchoFormDialog.vue)

原因：
- Rune 与 Echo 的字段语义已明显不同
- Echo 需要 `anno_source` 编辑体验
- 后续可能要加语法提示、示例模板、校验，不适合继续塞进 Rune 表单

EchoFormDialog 首版字段建议：
- 名称
- 描述
- 图标
- 颜色
- Monaco anno 源文本
- 只读示例说明：如何消费 `attrs` 和 `prompt`

## 数据流

```mermaid
flowchart TD
  settingsDialog[SettingsDialog] --> echoForm[EchoFormDialog]
  echoForm --> dbLayer[Electron DB IPC]
  dbLayer --> clientStore[client echoCards]
  clientStore --> muyaWrapper[Muya.vue]
  muyaWrapper --> echoRegistry[EchoRegistry]
  muyaWrapper --> quickInsert[QuickInsertProvider]
  markdownInput[UserMarkdown @回响名{...}(...) ] --> parserRule[Muya EchoRule]
  parserRule --> echoToken[EchoToken]
  echoToken --> echoRenderer[EchoInlineRenderer]
  echoRenderer --> echoRegistry
  echoRegistry --> echoRuntime[EchoRuntime]
  echoRuntime --> finalVNode[RenderedVNode or HostDOM]
```

## 实施顺序
1. 先修正 echo 数据模型与 DB 字段语义，保证后面 parser 接入时有稳定的回响定义来源。
2. 重构设置页与 EchoFormDialog，使 Monaco 内容切为 `anno_source`，并补保存后的重新 register。
3. 扩展 `@` 快速插入，将 echo 与 rune 并列接入。
4. 在 Muya parser 中增加 `echo_anno` 语法 token。
5. 实现 EchoRegistry / EchoRuntime / inline renderer。
6. 清理当前 `Muya.vue` 的 echo 字符串补丁逻辑，仅保留必要兼容迁移。
7. 做针对性验证：新增、编辑、删除、排序、快速插入、重新渲染、重新打开笔记后的持久化。

---

## 【2026-06-23 更新】回响与符文定位区分

### 新定位

| 类型 | 定位 | 模板格式 | 示例 |
|------|------|----------|------|
| **符文 (Rune)** | 装饰/渲染型 - 在当前位置渲染视觉元素 | Vue SFC 模板 (`<template>`, `<script>`, `<style>`) | 显示彩色文字、嵌入组件 |
| **回响 (Echo)** | 影响型 - 影响周围元素排版或执行功能 | JS 模块格式 (export default object) | 调整间距、添加边框、影响相邻节点样式 |

### 改动计划

#### 1. 回响 Reset Template 改为 JS 模块格式

**修改文件**：`src/components/ui/dialog/EchoFormDialog.vue`

**改动内容**：
- 移除对 `createDefaultRuneTemplate` 的依赖
- 新增独立的 `createDefaultEchoAnnoSource(name)` 函数
- 返回 JS 模块格式模板，包含：
  - `namespace` - 回响命名空间
  - `render(node, ancestors)` - 渲染钩子，返回 hast/vnode 结构
  - `afterRender(node, domElement, ancestors)` - 后渲染钩子，可修改周围节点
- Monaco 编辑器语言改为 `javascript`
- 更新提示文本说明可用钩子

**新模板示例**：
```javascript
// 回响模块 - 影响周围元素排版或执行功能
// context: { attrs, prompt, echo, name, node, ancestors }

export default {
  namespace: "回响",

  // 渲染钩子 - 返回该节点的 hast 结构
  render: (node, ancestors) => {
    return {
      tag: 'span',
      props: {
        class: 'echo-block',
        'data-echo-namespace': '回响',
        style: 'margin: 8px 0; padding: 4px 8px; border-left: 3px solid #26A69A;'
      },
      children: []
    }
  },

  // 后渲染钩子 - DOM 渲染完成后执行
  afterRender: (node, domElement, ancestors) => {
    // 可修改相邻节点样式
    const nextEl = domElement?.nextElementSibling;
    if (nextEl) {
      nextEl.style.marginTop = '16px';
    }
  }
};
```

#### 2. 后续扩展方向（待定）
- 提供更多预置钩子函数（如 `beforeRender`, `onClick`, `onHover`）
- 支持访问 `prevSibling` / `nextSibling` 操作相邻节点
- 提供工具函数导出（如 `getNanoId`, `renderVoidElement`）

### Muya 架构参考

- **渲染函数位置**：`src/libs/muya/lib/parser/render/renderInlines/`
- **关键参数**：
  - `h` - hyperscript 函数，用于创建 vnode
  - `cursor` - 光标状态
  - `block` - 当前块
  - `token` - 解析出的 token，包含 `echoName`, `echoId`, `attrsParsed`, `prompt`
  - `ancestors` - 祖先节点数组
- **现有实现参考**：`echoAnno.js` - 已有占位符渲染逻辑

## 关键风险与处理
- 语法复杂度：`attrs` 中可能包含引号、空格、数字、逗号，首版需限制解析范围并明确约定。
- Muya inline parser 兼容性：新增规则要避免与 link / html_tag / text 冲突，先放在合适优先级位置。
- 回响运行时过重：首版不追求完整复刻 coolma 全部 anno 生态，只实现当前产品需要的注册-解析-渲染闭环。
- 历史脏数据：现有 echo 的 `template` 需要迁移或回退兼容，防止设置页打开即报错。

## 参考依据
- coolma 语法与目标方向：[ui-javascript/coolma](https://github.com/ui-javascript/coolma)
- anno 模块组织参考：[demo-coolma-202205/_coolma/anno](https://github.com/ui-javascript/demo-coolma-202205/tree/master/_coolma/anno)
- 现有 Muya parser 入口：[src/libs/muya/lib/parser/index.js](src/libs/muya/lib/parser/index.js)
- 现有 quick insert 与编辑器封装：[src/components/ui/editor/Muya.vue](src/components/ui/editor/Muya.vue)
- 现有设置页入口：[src/components/ui/dialog/SettingsDialog.vue](src/components/ui/dialog/SettingsDialog.vue)