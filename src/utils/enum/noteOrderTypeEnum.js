/**
 * 笔记排序方式枚举
 *
 * 真源在 `src/utils/const/noteOrderTypesConst.js`（enum-plus 实现）。
 * 这里只是 re-export + 默认值，方便从 `src/utils/enum` 一行 import。
 */

export {
  NoteOrderTypeEnum,
  DEFAULT_NOTE_ORDER_TYPE
} from 'src/utils/const/noteOrderTypesConst'