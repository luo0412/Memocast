# TODO-本地线上文件冲突合并-202608

> 调研日期：2026-06-21
> 项目：Memocast (Electron + Vue2 + Quasar + SQLite)
> 目标：梳理适用于 Electron 桌面应用的成熟文件同步机制与冲突合并协议

---

## 一、背景：当前 Memocast 的同步架构

Memocast 当前采用**纯本地优先（Local-First）策略**：

- 所有用户编辑先写入本地 SQLite，`dirty=1` 标记待同步
- 同步默认只**推送**（backupToCloud），不主动拉取
- 拉取仅用于**恢复新笔记**，绝不覆盖本地已有内容
- **无冲突检测**，本地永远覆盖云端
- 同步触发为纯手动（用户点击"同步"按钮）

```
用户编辑 → dirty=1 → backupToCloud → 云端更新 → dirty=0
                               ↓
                    无匹配则创建新云端笔记
```

**核心设计哲学**：消灭冲突，而不是处理冲突。

---

## 二、成熟同步机制与协议全景图

### 2.1 协议层（同步语义）

| 协议/机制 | 核心思想 | 代表产品 | 适用场景 |
|-----------|----------|----------|----------|
| **OT (Operational Transformation)** | 将操作（insert/delete）变换后应用到各副本 | Google Docs, Etherpad | 实时协同编辑，字符级冲突解决 |
| **CRDT (Conflict-free Replicated Data Type)** | 数据结构天然无冲突，通过数学保证最终一致 | Yjs (Yjs.org), Automerge, Riak | 分布式协同，离线优先 |
| **Sync Protocol (Delta-based)** | 只同步差异（delta），基于状态向量/版本向量追踪变更 | rsync, Dropbox, Syncthing | 文件同步，按文件/块为单位 |
| **Event Sourcing + Vector Clock** | 每个事件带版本向量，冲突时靠业务规则解决 | Datomic, EventStore | 审计日志，需完整历史 |
| **Last-Write-Wins (LWW)** | 以时间戳或 Lamport 时钟决定最终值 | Cassandra, 简单 KV 存储 | 无需保留历史，最简单 |
| **Mergeable Persistent Data Structures** | 基于"三点合并"（3-way merge），Git 思想 | Git, ProseMirror | 文本内容，可视化合并 |

### 2.2 实现层（库/工具）

| 库/工具 | 类型 | 语言 | 成熟度 | 适合 Memocast 程度 |
|---------|------|------|--------|-------------------|
| **Yjs** | CRDT 框架 | JS/TS | 生产级（Notion 底层） | ★★★★★ |
| **Automerge 2** | CRDT 库 | JS/TS | 生产级 | ★★★★☆ |
| **Syncthing** | 文件同步工具+协议 | Go | 成熟开源 | ★★★☆☆（需嵌入） |
| **rsync 算法** | Delta 同步 | C | 非常成熟 | ★★★★☆（算法借鉴） |
| **Differential Synchronization** | 差分同步算法 | - | 学术+实践 | ★★★★☆ |
| **prosemirror-unified / TipTap** | 基于 OT 的编辑器 | JS | 生产级 | ★★★★★ |
| **Dotted Version Vectors** | 版本向量 | - | 学术 | ★★★☆☆ |

---

## 三、各机制深度解析

### 3.1 CRDT（Conflict-free Replicated Data Type）

**原理**：为每条数据赋予一个**唯一 ID + 逻辑时钟**，通过数学证明保证所有副本最终一致，无需中心协调。

**文本 CRDT（适合 Markdown 编辑器）**：
- 文本被分解为多个 **Position**（位置）节点，每个节点有唯一 ID
- 插入/删除操作基于 ID 而非绝对位置，避免"光标漂移"问题
- 离线编辑后重新联网，多方变更自动合并，无冲突

**代表实现：Yjs**
- 被 Notion、Liveblocks、Hocuspocus 等采用
- 支持 `Y.Text`、`Y.Map`、`Y.Array`
- 已有 Muya（Memocast 编辑器）的 Yjs 绑定
- `y-indexeddb` + `y-websocket` 组合实现离线优先 + 实时协同

```
Memocast 接入 Yjs 方案：
┌─────────────────┐     ┌──────────────────┐     ┌─────────────┐
│  Yjs (内存)     │────▶│  y-indexeddb     │────▶│  SQLite     │
│  Y.Doc          │     │  (本地持久化)    │     │  (存储Y.Doc) │
└─────────────────┘     └──────────────────┘     └─────────────┘
        │
        ▼
┌──────────────────┐     ┌──────────────────┐
│  y-websocket     │────▶│  Cloud Server    │
│  (Provider)      │     │  (WizNote API?)  │
└──────────────────┘     └──────────────────┘
```

**CRDT 的优势**：
- 冲突不是异常，而是数据结构的一部分
- 天然支持离线编辑
- 不需要中心服务器做协调
- 已在 Markdown 编辑器（Muya/TIPTap）验证

**CRDT 的挑战**：
- 对 blob（二进制附件）支持不如文本
- 内存占用随文档大小增长（需压缩/GC）
- 云端存储格式与当前 SQLite 不兼容
- WizNote API 不支持 CRDT 语义

### 3.2 OT（Operational Transformation）

**原理**：将编辑操作（insert/delete）抽象为可组合、可变换的指令，客户端发送操作到服务器，服务器将操作"变换"后再广播给其他人。

**代表实现**：
- Google Docs：字符级 OT
- TipTap / ProseMirror：基于 OT 的协同框架
- ShareDB：Node.js OT 框架

**OT vs CRDT 对比**：

| 维度 | OT | CRDT (Yjs) |
|------|----|------------|
| 协调者需求 | 需要中心服务器处理变换 | 无中心，全对等 |
| 离线支持 | 弱（需重连后变换） | 强（可离线合并） |
| 实现复杂度 | 高（变换函数正确性难证明） | 中（数学保证） |
| 内存占用 | 低（只存操作） | 较高（存完整结构） |
| 生态成熟度 | 高（15年+） | 高（快速增长） |
| Memocast 适配 | 需改造 SyncService | 可作为独立协同层 |

### 3.3 Delta Sync（增量同步 / Rsync 算法）

**原理**：只传输文件/数据库的差异部分（delta），而非整个文件。

```
1. 客户端发送文件的签名块列表（类似 rolling hash）
2. 服务器找出本地与云端匹配的块，跳过未变化的块
3. 只传输变化的部分
4. 服务端重组文件
```

**应用场景**：
- 大型数据库文件（如 SQLite `.db`）的增量备份
- 大型 Markdown 笔记的差分同步
- WizNote API 的草稿版本管理

**Memocast 可借鉴**：
- `notes` 表的 `local_modified` / `server_modified` 时间戳差异 = Delta
- 可扩展为：只同步 `dirty=1` 且 `local_modified > server_modified` 的记录
- 比当前全量推送更高效

### 3.4 Last-Write-Wins（LWW，最后写入胜出）

**原理**：以时间戳（或 Lamport 时钟）决定哪个版本是"正确答案"，时间戳较新的覆盖旧的。

**Memocast 当前某种程度上就是 LWW**：
```javascript
// 当前行为：本地永远覆盖云端（时间戳由本地生成）
local_modified = now
dirty = 1
// 云端修改？忽略
```

**升级为双向 LWW**：
```javascript
// 如果要支持云端优先编辑
if (server_modified > local_modified) {
  // 云端更新，本地接受
  pullFromCloud()
} else {
  // 本地更新，推送到云端
  pushToCloud()
}
```

**风险**：快速连续编辑（< 1s）会导致数据丢失。

### 3.5 Mergeable Persistent Data Structures（Git 风格三点合并）

**原理**：类似 Git 的 3-way merge，给定 base、local、remote，自动化合并。

```
       base (共同祖先)
      /            \
  local          remote
      \            /
       └────┬────┘
         merge
```

**适用于**：
- Markdown 文本内容（行级或块级 merge）
- 分类/标签树结构
- 数据库 schema 迁移

**Memocast 可借鉴**：
- 对 `title`、`category` 字段做 3-way merge
- 对 `content`（Markdown）做行级 diff + merge
- 对 `tags` 做集合 union

**实现库**：
- `diff-match-patch`（Google）：文本 diff + patch
- `node-merge`(git 风格的 3-way merge)
- `markserv` / `markdown-it` 插件（行级合并）

### 3.6 Vector Clocks（向量时钟）

**原理**：每个节点维护一个向量时钟 `{node_a: 3, node_b: 2}`，记录每个节点的逻辑时间。通过比较向量时钟判断因果关系（先后/并发）。

```
客户端本地时钟: {note_edit: 5}
云端服务器时钟: {note_edit: 4}

比较结果: 本地时钟 > 云端 → 接受本地修改（因果一致）
```

**适用场景**：
- 判断编辑操作是否"因果相关"（同一笔记的串行编辑 vs 并发编辑）
- 配合 CRDT 使用，提供更细粒度的冲突检测

---

## 四、Electron 项目的特殊考量

### 4.1 主进程 vs 渲染进程

```
┌─────────────────────────────────────────────────────┐
│  渲染进程 (Renderer)                                 │
│  ┌─────────────┐    ┌──────────────┐                │
│  │  Muya Editor │    │  Vuex Store   │                │
│  │  (Yjs 绑定)  │    │  (note state) │                │
│  └──────┬──────┘    └──────┬───────┘                │
│         │                  │                        │
│         └────────┬─────────┘                        │
│                  │ IPC                               │
└──────────────────┼──────────────────────────────────┘
                   ▼
┌──────────────────────────────────────────────────────┐
│  主进程 (Main)                                       │
│  ┌────────────────┐    ┌──────────────────┐         │
│  │  SQLite (sql.js)│    │  SyncService      │         │
│  │  (notes表)      │    │  (backup/restore) │         │
│  └────────────────┘    └──────────────────┘         │
│                                │                     │
│                                ▼                     │
│                    ┌──────────────────┐              │
│                    │  WizNote API      │              │
│                    │  (云端存储)        │              │
│                    └──────────────────┘              │
└──────────────────────────────────────────────────────┘
```

**关键问题**：Yjs CRDT 层应该在渲染进程，SyncService 在主进程，需要 IPC 桥接。

### 4.2 SQLite + CRDT 共存方案

**方案 A：双存储并存（推荐）**
```
SQLite (当前)           Yjs (新增)
     │                      │
     ▼                      ▼
原始笔记内容          CRDT 文档（Y.Doc）
(备用于导出/兼容)    (用于协同和冲突合并)
```

**方案 B：纯 CRDT 替换**
- 将 `content` 字段替换为 Yjs 序列化数据
- 需要重建数据库迁移脚本
- 风险较高

### 4.3 主进程 IPC 链路（参考现有设计）

```
Renderer: notePersistenceService.js
    │
    │ ipcRenderer.invoke('db:note:update', ...)
    ▼
Main: electron-main.js (db:note:update handler)
    │
    ▼
SQLite: notes 表 (dirty, local_modified, server_modified)
    │
    ▼ (if sync triggered)
SyncService.backupToCloud()
    │
    ▼
WizNote REST API
```

---

## 五、实施路径（Phase 1 + Phase 2 合并执行）

> **重要更新**：路径一（轻量冲突感知）和路径二（Yjs 引入）不再是独立的二选一路径，而是**同一个方案的两期迭代**。详见第六节"最终决策"。

### 路径一：轻量增强（最小改动，推荐先行）

**目标**：在现有架构上增加"感知冲突"的能力，不改变核心语义。

```
┌──────────────────────────────────────────────────────────────────┐
│  改动范围                                                        │
│  1. notes 表增加 conflict_backup 列（冲突前快照）                  │
│  2. SyncService 增加冲突检测：local_modified ≠ server_modified     │
│  3. UI 增加冲突处理面板（三栏：本地 / 云端 / 合并结果）             │
│  4. 保持 dirty 字段语义不变                                        │
└──────────────────────────────────────────────────────────────────┘
```

**改动文件**：
- `src-electron/main-process/electron-main.js` — 新增 conflict_backup 表、冲突检测逻辑
- `src/services/SyncService.js` — 增加冲突分支
- `src/components/ui/dialog/ConflictResolveDialog.vue` — 新建冲突处理 UI
- `src/i18n/zh-cn/other.js` / `en-us/other.js` — 国际化
- `src/utils/constants.js` — 冲突相关常量

**冲突处理策略**：
- 检测到 `local_modified ≠ server_modified` 且均 `> 0` 时 → 触发冲突
- 将当前内容存入 `conflict_backup`，展示三方 Diff（本地 / 云端 / 合并）
- 用户选择后，清除冲突状态，`dirty=1` 重新同步

---

### 路径二：引入 CRDT（Yjs）

**目标**：用 Yjs CRDT 层彻底解决文本冲突。

```
改动范围（中等）：
  1. 安装 yjs, y-indexeddb, y-websocket
  2. 改造 Muya 编辑器，绑定 Y.Text
  3. Yjs 文档持久化到 SQLite（或 IndexedDB）
  4. SyncService 改为同步 Yjs Update（二进制 diff）
  5. 云端存储 Yjs Update Stream（而非原文）
```

**改动文件**：
- `package.json` — 新增 yjs 依赖
- `src/libs/muya/lib/utils/exportMarkdown.js` — Yjs 序列化/反序列化
- `src/store/server/notePersistenceService.js` — Yjs 文档管理
- `src/services/SyncService.js` — Yjs Update 推送/拉取
- `src/services/CloudSyncService.js` — UI 层 Yjs 状态

**关键挑战**：
- WizNote API 不支持 Yjs 语义，需在云端存储 Yjs 二进制
- 需自建 Gateway 或扩展 WizNote 服务器
- Yjs GC 和内存管理需处理

---

### 路径三：引入 OT（TipTap 替换 Muya）

**目标**：用 TipTap（基于 Yjs）替换 Muya，获得完整协同生态。

```
改动范围（较大）：
  1. 移除 Muya，引入 TipTap + Yjs
  2. 重建笔记加载/保存逻辑
  3. 改造 notePersistenceService
  4. SyncService 改为 Yjs sync
```

**风险**：Muya 是 Memocast 编辑器的核心依赖，替换成本极高。

---

### 路径四：Serverless CRDT Gateway（混合架构）

**目标**：在 Memocast 和 WizNote 之间加一层 CRDT 网关。

```
┌──────────────┐     ┌──────────────────┐     ┌──────────────────┐
│  Memocast    │────▶│  CRDT Gateway    │────▶│  WizNote API     │
│  (Yjs 客户端) │     │  (Cloudflare     │     │  (存储笔记原文)   │
│              │     │   Workers/Node)   │     │                  │
└──────────────┘     └──────────────────┘     └──────────────────┘
```

**网关职责**：
- 接收 Yjs Update（二进制 diff）
- 合并后存储到 WizNote
- 下发变更给其他客户端

**优势**：不依赖 WizNote 支持 CRDT
**劣势**：需自建并维护网关服务

---

## 六、最终决策：单用户多设备 + 预留协同

### 决策背景

冲突场景本质是 **单用户多设备**（Desktop + Mobile/Web 的同一账号分别编辑后同步），而不是多用户实时协同编辑。这两个问题的复杂度差一个数量级：

- **多用户实时协同**：多人同时在线编辑同一文档 → 需要 CRDT 解决操作顺序不确定问题
- **单用户多设备**：同一账号在不同设备上分时编辑后同步 → 只需文本智能合并 + 用户确认

### 为什么选 Yjs 而不是纯 LWW

对于单用户多设备场景，纯 LWW（以时间戳决定谁覆盖谁）理论上够用，但有两个痛点：

1. **快速连续编辑（< 1s）会导致数据丢失**，用户感知差
2. **无法智能合并**——两个人在不同段落下手，LWW 会丢失一方；CRDT 能自动合并

更关键的是，Yjs 的增量引入成本不高：

- `@muya/integration-yjs` 已存在，Muya 天然支持 Yjs 绑定
- `y-indexeddb` 天然解决离线优先 + 冲突合并两个问题
- 明年做协同办公时，底层设施已就绪，不需要重构

### 最终方案：Phase 1 + Phase 2 合并执行

不再把路径一和路径二视为两个独立阶段，而是视为**同一套方案的两次迭代**：

```
Phase 1（1-2 周）：冲突感知 + 三方 Diff UI
  → 解决当前痛点，改动小，见效快
  → 为 Phase 2 奠定数据库基础

Phase 2（2-4 周）：引入 Yjs 文本层
  → 让冲突合并更智能（CRDT 自动合并非冲突区域）
  → 为明年协同办公铺路
  → Yjs Update 推送到云端（WizNote 扩展字段或自建 Gateway）

Phase 3（长期）：完善协同生态
  ├── 自建 CRDT Gateway（或扩展 WizNote）
  ├── 标签/分类的 CRDT 化（Y.Map）
  ├── 二进制附件的 Delta Sync
  └── 多端实时协同（可选）
```

> **结论**：不要因为"明年才要协同"就跳过 CRDT。Yjs 解决的恰恰也是当前的单用户多设备冲突合并问题，一次接入，长期收益。

---

### 兼容设计：本地优先为根基，CRDT 为可选增强层

#### 核心定位

**本地优先**（Local-first）是 Memocast 的核心设计哲学，永远是默认值和根基。**CRDT**（Yjs）是一个可选的增强层，建立在本地优先之上，不是替代关系：

```
用户感知层
  ├── 基础模式（默认）：本地优先 + 冲突检测 + 三方 Diff UI
  └── 增强模式（可选）：本地优先 + Yjs CRDT + 智能合并

核心数据层（永远不变）
  └── SQLite 是本地权威，notes.content 是最终状态
```

这意味着：
- **无论开启哪个模式**：本地 SQLite 始终是数据权威，`notes.content` 始终是最新的 Markdown 文本
- **本地优先不变**：云端永远不能主动覆盖本地内容，必须经过用户确认
- **CRDT 是传输层优化**：Yjs 只影响"云端如何接收和合并更新"，不影响本地存储语义

#### 三种可选模式的对比

| 维度 | 基础模式（默认） | 增强模式（Yjs CRDT） |
|------|-----------------|---------------------|
| **定位** | 本地优先 + 手动冲突解决 | 本地优先 + Yjs 自动合并 |
| **数据权威** | SQLite（不变） | SQLite（不变） |
| **冲突处理** | 三方 Diff UI，用户手动决策 | CRDT 自动合并 + 降级 UI |
| **传输效率** | 全量 Markdown 文本 | 增量 Yjs Update 二进制 |
| **离线体验** | 完整保留 | 完整保留 |
| **云端要求** | WizNote 原生 API | 需要 Gateway 或 API 扩展 |

#### 切换逻辑

```
用户开启"增强模式"（Yjs）
    │
    ├── 已有笔记 content_yjs = NULL → 初始化 Y.Doc from notes.content
    ├── 已有笔记 content_yjs 有值 → 直接加载 Y.Doc
    └── 笔记内容始终从 notes.content 读取（向下兼容）

用户关闭"增强模式"
    │
    ├── Y.Doc 导出 → notes.content
    ├── content_yjs 保留（下次开启可恢复）
    └── notes.content 继续作为唯一数据源
```

#### 共用基础设施

```
┌─────────────────────────────────────────────────────────────┐
│  同一张 conflict_log 表                                      │
│  同一套 ConflictResolveDialog.vue                           │
│  同一个 SyncService.pushToCloud()                           │
│  同一张 notes 表（content / content_yjs / has_conflict）   │
└─────────────────────────────────────────────────────────────┘
```

**Phase 1 始终是兜底**：无论哪种模式，真正无法自动合并的冲突，都会走 Phase 1 的 `conflict_log` → `ConflictResolveDialog` 流程。

#### 数据库迁移

```sql
-- 新增字段（Phase 2 完成后追加）
ALTER TABLE notes ADD COLUMN content_yjs BLOB;

-- 用户设置表（新增）
CREATE TABLE IF NOT EXISTS sync_settings (
  id INTEGER PRIMARY KEY,
  kb_guid TEXT UNIQUE,
  crdt_enabled INTEGER DEFAULT 0,   -- 0=基础模式（默认），1=增强模式（Yjs）
  auto_merge INTEGER DEFAULT 1,      -- 是否自动合并非冲突区域
  updated_at INTEGER
);
```

#### SyncService 双模式分支

```javascript
async _getCrdtMode(kbGuid) {
  const settings = await DatabaseClient.syncSettings.get(kbGuid)
  return settings?.crdt_enabled === 1  // 默认为 false（基础模式）
}

async pushToCloud() {
  const kbGuid = getKbGuid()
  const pendingNotes = await DatabaseClient.notes.getPendingByKbGuid(kbGuid)
  const stats = { pushed: 0, conflicts: 0, errors: 0, skipped: 0, backfilled: 0 }
  const useCrdt = await this._getCrdtMode(kbGuid)

  for (const note of pendingNotes) {
    const hasCloudGuid = note.doc_guid && !note.doc_guid.startsWith('local_')

    if (hasCloudGuid) {
      // ── 增强模式（Yjs CRDT）优先 ──
      if (useCrdt && note.content_yjs) {
        const result = await this._pushYjsUpdate(note)
        if (result.pushed) { stats.pushed++; continue }
        // Yjs 推送失败 → 降级到基础路径（不阻塞）
      }

      // ── 基础模式（内容层）+ 增强模式降级路径 ──
      const conflictResult = await this._detectAndLogConflict(note)
      if (conflictResult.hasConflict) {
        stats.conflicts++
        stats.skipped++
        continue
      }

      // 无冲突 → 正常推送
      await api.updateDoc(note.doc_guid, {
        title: note.title,
        content: note.content,
        category: note.category,
        tags: note.tags
      }, kbGuid)
      await DatabaseClient.notes.update(note.id, {
        dirty: 0, server_modified: Date.now()
      }, { isSystemUpdate: true })
      stats.pushed++
    } else {
      // local_ 前缀笔记：无冲突，正常创建或匹配
      // ...（原有逻辑不变）...
    }
  }

  return stats
}

async _pushYjsUpdate(note) {
  try {
    const lastVector = note.last_sync_state_vector
      ? new Uint8Array(note.last_sync_state_vector) : null
    const yjsUpdate = await yjsBridge.getYjsUpdateSince(note.doc_guid, lastVector)
    if (!yjsUpdate || yjsUpdate.length === 0) return { pushed: false }

    await api.pushYjsUpdate(note.doc_guid, Array.from(yjsUpdate), note.kb_guid)
    const newVector = Y.encodeStateVector(yjsBridge.getYDoc(note.doc_guid))
    await DatabaseClient.notes.update(note.id, {
      last_sync_state_vector: Array.from(newVector),
      dirty: 0, server_modified: Date.now()
    }, { isSystemUpdate: true })
    return { pushed: true }
  } catch (err) {
    console.warn(`[SyncService] Yjs push failed:`, err.message)
    return { pushed: false }
  }
}
```



---



---

## 七、最终决策点

经过分析，以下问题已有明确答案：

1. **是否需要多端实时协同？**
   - **当前目标**：单用户多设备分时编辑，冲突合并为主
   - **明年目标**：协同办公（与当前 Phase 2 Yjs 引入不冲突，反而复用基础设施）

2. **是否可修改 WizNote 服务端？**
   - **不可以** → 需要自建 Gateway 或在 WizNote API 扩展字段中存储 Yjs Update 二进制

3. **Memocast 是否保持离线优先？**
   - **是**，离线优先是 Memocast 的核心设计哲学
   - Yjs `y-indexeddb` Provider 天然匹配此需求，与现有 dirty 字段架构可共存

4. **对"冲突"的定义？**
   - 采用**双层策略**：
     - **自动层**：Yjs CRDT 自动合并非冲突区域（如两个人在不同段落下手）
     - **手动层**：真正的冲突（同一区域被双方修改） → 三方 Diff UI 交给用户决定
   - 快速连续编辑（< 1s）由 Yjs CRDT 保证不丢失，LWW 无法做到这一点

---

### Phase 1 详细实施计划（1-2 周）

#### 目标

在现有架构上增加冲突感知能力，检测冲突时展示三方 Diff UI，由用户手动解决。

---

#### 一、数据库改动

##### 1.1 新增 `conflict_log` 表

参照现有 `sync_log` 表的设计风格（在 `initSchema` 中通过 `CREATE TABLE IF NOT EXISTS` 创建），新增冲突日志表：

```425:443:electron-main.js (参考现有 sync_log 表写法)
CREATE TABLE IF NOT EXISTS conflict_log (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  note_id INTEGER NOT NULL,
  doc_guid TEXT,
  kb_guid TEXT,
  local_content TEXT,
  server_content TEXT,
  local_modified INTEGER,
  server_modified INTEGER,
  local_title TEXT,
  server_title TEXT,
  resolved INTEGER DEFAULT 0,
  resolved_strategy TEXT CHECK(resolved_strategy IN ('local_wins', 'server_wins', 'merged')),
  resolved_content TEXT,
  resolved_at INTEGER,
  created_at INTEGER NOT NULL,
  FOREIGN KEY (note_id) REFERENCES notes(id) ON DELETE CASCADE
);
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| `note_id` | INTEGER | 关联的本地笔记 ID，外键级联删除 |
| `doc_guid` | TEXT | 云端文档 GUID，便于 UI 展示和重推 |
| `kb_guid` | TEXT | 知识库 GUID |
| `local_content` | TEXT | 冲突时本地版本的完整内容快照 |
| `server_content` | TEXT | 冲突时云端版本的完整内容快照 |
| `local_modified` | INTEGER | 本地修改时间戳（毫秒） |
| `server_modified` | INTEGER | 云端修改时间戳（毫秒） |
| `local_title` | TEXT | 冲突时本地标题（便于 UI 展示） |
| `server_title` | TEXT | 冲突时云端标题 |
| `resolved` | INTEGER | 是否已解决（0=未解决，1=已解决） |
| `resolved_strategy` | TEXT | 解决策略：`local_wins` / `server_wins` / `merged` |
| `resolved_content` | TEXT | 用户手动合并后的内容（仅 `merged` 时有值） |
| `resolved_at` | INTEGER | 解决时间戳 |
| `created_at` | INTEGER | 冲突记录创建时间 |

**设计考量：**
- 不存 `resolved=0` 时对应 `conflict_log.resolved_content = NULL`（未合并），存 `resolved_content` 仅在 `merged` 时有值
- `local_title` / `server_title` 单独存储，因为标题也可能被双方修改
- `resolved` 用 INTEGER 而非 BOOLEAN，便于未来扩展更多状态

##### 1.2 新增索引

```sql
-- 快速查询某笔记是否有未解决冲突
CREATE INDEX IF NOT EXISTS idx_conflict_log_note_id ON conflict_log(note_id);
CREATE INDEX IF NOT EXISTS idx_conflict_log_resolved ON conflict_log(resolved);
```

##### 1.3 `notes` 表增加字段

```sql
ALTER TABLE notes ADD COLUMN has_conflict INTEGER DEFAULT 0;
ALTER TABLE notes ADD COLUMN conflict_id INTEGER REFERENCES conflict_log(id);
```

> **注意**：`ALTER TABLE ... ADD COLUMN` 在 sql.js 中支持良好（现有代码已有多次 ALTER TABLE 迁移），无需重建表。

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| `has_conflict` | INTEGER | 0=无冲突，1=有未解决冲突 |
| `conflict_id` | INTEGER | 指向 `conflict_log.id`，未冲突时为 NULL |

##### 1.4 `initSchema()` 改动位置

参照现有迁移写法，在 `initSchema()` 末尾（`sync_log` 表创建之后）追加迁移逻辑：

```javascript
// 现有代码（约 line 420-450）：
// 处理 kb_guid 迁移、dirty 迁移、sync_log 表等

// 新增：conflict_log 表（仅在不存在时创建，兼容已有数据库）
const conflictLogTableExists = execOne(
  "SELECT name FROM sqlite_master WHERE type='table' AND name='conflict_log'"
)
if (!conflictLogTableExists) {
  execRun(`
    CREATE TABLE IF NOT EXISTS conflict_log (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      note_id INTEGER NOT NULL,
      doc_guid TEXT,
      kb_guid TEXT,
      local_content TEXT,
      server_content TEXT,
      local_modified INTEGER,
      server_modified INTEGER,
      local_title TEXT,
      server_title TEXT,
      resolved INTEGER DEFAULT 0,
      resolved_strategy TEXT CHECK(resolved_strategy IN ('local_wins', 'server_wins', 'merged')),
      resolved_content TEXT,
      resolved_at INTEGER,
      created_at INTEGER NOT NULL,
      FOREIGN KEY (note_id) REFERENCES notes(id) ON DELETE CASCADE
    )
  `)
  execRun(`CREATE INDEX IF NOT EXISTS idx_conflict_log_note_id ON conflict_log(note_id)`)
  execRun(`CREATE INDEX IF NOT EXISTS idx_conflict_log_resolved ON conflict_log(resolved)`)
}

// 新增：notes.has_conflict / conflict_id 列（仅在不存在时添加，兼容已有数据库）
const hasConflictCol = execOne(
  "SELECT name FROM pragma_table_info('notes') WHERE name='has_conflict'"
)
if (!hasConflictCol) {
  execRun(`ALTER TABLE notes ADD COLUMN has_conflict INTEGER DEFAULT 0`)
  execRun(`ALTER TABLE notes ADD COLUMN conflict_id INTEGER REFERENCES conflict_log(id)`)
}
```

---

#### 二、IPC Handlers 详细设计

参照现有 IPC handler 的命名规范（`db:getXxx`、`db:createXxx`、`db:updateXxx`），新增以下 handlers：

##### 2.1 新增 handlers 列表

| Handler | 方法 | 输入参数 | 返回值 | 说明 |
|---------|------|----------|--------|------|
| `db:createConflictLog` | INSERT | `{noteId, docGuid, kbGuid, localContent, serverContent, localModified, serverModified, localTitle, serverTitle}` | `{id: number}` | 创建冲突日志，返回新记录 ID |
| `db:getConflictLog` | SELECT | `{conflictId}` | `ConflictLog` 对象或 null | 获取单条冲突记录 |
| `db:getUnresolvedConflicts` | SELECT | `{kbGuid}` | `ConflictLog[]` | 获取某账户所有未解决冲突 |
| `db:resolveConflict` | UPDATE | `{conflictId, strategy, resolvedContent}` | `{success: boolean}` | 解决冲突（更新 resolved 相关字段） |
| `db:resolveConflictsBatch` | UPDATE | `{conflictIds: number[], strategy, resolvedContent}` | `{count: number}` | 批量解决冲突（用于"全部以本地为准"等场景） |
| `db:getConflictNote` | SELECT | `{noteId}` | `ConflictLog` 或 null | 获取某笔记的最新冲突记录（未解决） |
| `db:markNoteConflict` | UPDATE | `{noteId, conflictId}` | `{success: boolean}` | 标记笔记有冲突（写入 has_conflict + conflict_id） |
| `db:clearNoteConflict` | UPDATE | `{noteId}` | `{success: boolean}` | 清除笔记冲突标记（解决后调用） |
| `db:getConflictStats` | SELECT | `{kbGuid}` | `{total: number, unresolved: number}` | 获取冲突统计（显示在同步按钮旁） |

##### 2.2 核心 handler 实现示例

**`db:createConflictLog`（最关键的写操作）：**

```javascript
// electron-main.js，约新增在 line ~1750（sync handlers 之后）
ipcMain.handle('db:createConflictLog', async (event, params) => {
  const {
    noteId, docGuid, kbGuid,
    localContent, serverContent,
    localModified, serverModified,
    localTitle, serverTitle
  } = params

  // 先检查该笔记是否已有未解决的冲突记录（避免重复创建）
  const existing = execOne(
    `SELECT id FROM conflict_log WHERE note_id = ? AND resolved = 0`,
    [noteId]
  )
  if (existing) {
    return { id: existing.id, duplicate: true }
  }

  execRun(`
    INSERT INTO conflict_log
      (note_id, doc_guid, kb_guid, local_content, server_content,
       local_modified, server_modified, local_title, server_title, created_at)
    VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
  `, [
    noteId, docGuid || null, kbGuid || null,
    localContent || '', serverContent || '',
    localModified || 0, serverModified || 0,
    localTitle || '', serverTitle || '',
    Date.now()
  ])

  const id = getLastInsertRowid()
  return { id }
})
```

**`db:resolveConflict`：**

```javascript
ipcMain.handle('db:resolveConflict', async (event, params) => {
  const { conflictId, strategy, resolvedContent } = params

  if (!['local_wins', 'server_wins', 'merged'].includes(strategy)) {
    throw new Error(`Invalid strategy: ${strategy}`)
  }
  if (strategy === 'merged' && !resolvedContent) {
    throw new Error('merged strategy requires resolvedContent')
  }

  execRun(`
    UPDATE conflict_log
    SET resolved = 1,
        resolved_strategy = ?,
        resolved_content = ?,
        resolved_at = ?
    WHERE id = ? AND resolved = 0
  `, [strategy, resolvedContent || null, Date.now(), conflictId])

  // 同步清除关联 notes 的 has_conflict 标记（由调用方保证原子性）
  return { success: true }
})
```

**`db:getUnresolvedConflicts`：**

```javascript
ipcMain.handle('db:getUnresolvedConflicts', async (event, kbGuid) => {
  const sql = `
    SELECT cl.*, n.title as note_title, n.category as note_category
    FROM conflict_log cl
    LEFT JOIN notes n ON cl.note_id = n.id
    WHERE cl.resolved = 0
      ${kbGuid ? 'AND cl.kb_guid = ?' : ''}
    ORDER BY cl.created_at DESC
  `
  const params = kbGuid ? [kbGuid] : []
  return execToObjects(sql, params)
})
```

---

#### 三、SyncService 冲突检测逻辑

##### 3.1 双模式架构

SyncService 内部根据 `sync_mode` 分支处理：

```
SyncService.pushToCloud()
    │
    ├── sync_mode === 'content'（Phase 1 基础模式）
    │   ├── 遍历 pendingNotes
    │   ├── 对比 notes.content vs server.content
    │   ├── 检测到冲突 → conflict_log → ConflictResolveDialog
    │   └── 无冲突 → api.updateDoc(content) 推送 Markdown 文本
    │
    ├── crdt_enabled === true（增强模式 Yjs）
        ├── 遍历 pendingNotes
        ├── 计算 Yjs Update 增量（Yjs 已自动合并非冲突区域）
        ├── Yjs 自动合并不了 → 降级基础模式路径
        ├── Yjs 自动合并成功 → api.pushYjsUpdate()
        └── 降级 → ConflictResolveDialog（与基础模式共用 UI）
```

**关键原则**：本地 SQLite 始终是数据权威。基础模式和增强模式共用同一套 `conflict_log` 表、`ConflictResolveDialog`、`SyncService`，增强模式是基础模式之上的可选增强层。

##### 3.2 冲突检测条件（Phase 1 模式）

```javascript
// 冲突条件（需同时满足）：
// 1. 云端笔记存在（server_modified > 0）→ 说明不是首次创建
// 2. 本地修改时间 > 云端修改时间 → 说明本地有更新，云端也有更新（并发修改）
// 3. 当前笔记已关联云端 doc_guid（不是 local_* 前缀）
```

**注意**：现有代码中 `pushToCloud` 会先判断 `hasCloudGuid`（`doc_guid` 是否以 `local_` 开头），冲突检测只针对已关联云端 GUID 的笔记。新建笔记不存在冲突问题。

##### 3.3 改动代码（pushToCloud 中的关键片段）

```javascript
// src/services/SyncService.js，约 line 660-780

// 遍历 pendingNotes 部分
for (const note of pendingNotes) {
  const hasCloudGuid = note.doc_guid && !note.doc_guid.startsWith('local_')

  if (hasCloudGuid) {
    // ══════════════════════════════════════════════
    // 改动：冲突检测（仅对已关联云端 GUID 的笔记生效）
    // ══════════════════════════════════════════════
    try {
      const serverNote = await api.getNote(note.doc_guid)
      if (serverNote && serverNote.data_modified > 0) {
        const serverModified = serverNote.data_modified  // 云端时间戳
        const localModified = note.local_modified         // 本地时间戳

        // 冲突：本地和云端均有更新（并发修改）
        if (localModified > serverModified) {
          // 写入 conflict_log
          const conflictResult = await DatabaseClient.conflict.create({
            noteId: note.id,
            docGuid: note.doc_guid,
            kbGuid: note.kb_guid,
            localContent: note.content,
            serverContent: serverNote.content || '',
            localModified: localModified,
            serverModified: serverModified,
            localTitle: note.title,
            serverTitle: serverNote.title || note.title
          })

          // 标记笔记有冲突
          await DatabaseClient.notes.markConflict(note.id, conflictResult.id)

          // 通知 UI 层展示冲突处理面板
          this.notifyListeners({
            type: 'conflict_detected',
            conflictId: conflictResult.id,
            noteId: note.id,
            noteTitle: note.title,
            docGuid: note.doc_guid
          })

          stats.conflicts = (stats.conflicts || 0) + 1
          stats.skipped++   // 不走正常推送流程
          continue           // 跳过本次推送，下同
        }
      }
    } catch (err) {
      // 网络错误：跳过此笔记，记录错误，不阻塞其他笔记
      console.warn(`[SyncService] conflict detection failed for note ${note.id}:`, err.message)
      stats.errors++
      continue
    }
    // ══════════════════════════════════════════════
    // 原逻辑继续：正常推送
    // ══════════════════════════════════════════════

    syncMode = 'update-by-guid'
    cloudDocGuid = note.doc_guid
    await api.updateDoc(cloudDocGuid, { title, content, category, tags }, kbGuid)
  } else {
    // local_ 前缀笔记不存在冲突问题，正常创建/匹配
    // ...（原有逻辑不变）
  }
  // ...（后续保存 doc_guid、dirty=0 等逻辑不变）
}
```

##### 3.4 stats 对象扩展

现有 `stats` 对象为 `{pulled, pushed, errors, skipped, backfilled}`，新增：

```javascript
stats.conflicts = 0  // 检测到的冲突数量
```

##### 3.5 冲突通知事件

在 `notifyListeners` 的事件类型中新增：

| 事件类型 | 触发时机 | 携带数据 |
|----------|----------|----------|
| `conflict_detected` | 检测到冲突，写入 conflict_log 后 | `{conflictId, noteId, noteTitle, docGuid}` |

CloudSyncService 通过 `addListener` 接收此事件，弹出 `ConflictResolveDialog`。

##### 3.6 重新推送逻辑（解决冲突后）

用户解决冲突后，需要将 `resolved_content`（或 `local_content` / `server_content`）写回 notes 表，然后触发重新同步：

```javascript
// 用户解决冲突后，在 CloudSyncService 中处理
async resolveConflictAndSync({ conflictId, noteId, strategy, resolvedContent }) {
  // 1. 从 conflict_log 获取解决后的内容
  const conflict = await DatabaseClient.conflict.get(conflictId)

  // 2. 决定最终内容
  let finalContent
  if (strategy === 'local_wins') {
    finalContent = conflict.local_content
  } else if (strategy === 'server_wins') {
    finalContent = conflict.server_content
  } else {
    finalContent = resolvedContent  // 用户手动合并内容
  }

  // 3. 更新 notes 表（content + 标题 + dirty=1）
  await DatabaseClient.notes.update(noteId, {
    content: finalContent,
    title: strategy === 'server_wins' ? conflict.server_title : conflict.local_title,
    dirty: 1,
    has_conflict: 0,
    conflict_id: null,
    local_modified: Date.now()  // 更新本地时间戳，触发重新同步
  }, { isSystemUpdate: true })

  // 4. 解决 conflict_log
  await DatabaseClient.conflict.resolve(conflictId, strategy, resolvedContent)

  // 5. 清除笔记冲突标记
  await DatabaseClient.notes.clearConflict(noteId)

  // 6. 触发重新同步
  return await this.pushOnly()
}
```

---

#### 四、CloudSyncService 改动

##### 4.1 改动点

现有 `CloudSyncService` 是 `SyncService` 的 UI 封装，通过 `addListener` 接收 `SyncService` 的事件。只需改动 3 处：

1. **新增 `_status.conflicts` 字段**（跟踪未解决冲突数）
2. **在 `pushOnly()` 的 `_normalizeSyncResult` 中提取 `stats.conflicts`**
3. **在 `init()` 中查询并加载 `conflictStats`**
4. **新增 `resolveConflict()` 公开方法**

##### 4.2 改动详情

```javascript
// src/services/CloudSyncService.js

// 1. _status 扩展（line ~15）
this._status = {
  isSyncing: false,
  lastSyncTime: null,
  total: 0,
  synced: 0,
  pending: 0,
  conflicts: 0,     // 新增：未解决冲突数
  account: null,
  error: null
}

// 2. pushOnly() 结果中提取 conflicts（line ~220）
const stats = result.stats || {}
this._status.conflicts = stats.conflicts || 0

// 3. init() 中加载冲突统计（line ~244）
async init() {
  const stats = await DatabaseClient.sync.getStats()
  const conflictStats = await DatabaseClient.conflict.getStats(getKbGuid())
  this._status.total = stats.total || 0
  this._status.synced = stats.synced || 0
  this._status.pending = stats.pending || 0
  this._status.conflicts = conflictStats.unresolved || 0  // 新增
  this._status.lastSyncTime = (await DatabaseClient.appState.get('lastSyncTime')) || null
  this._status.account = getKbGuid()
}

// 4. 新增：冲突解决并重新同步
async resolveConflictAndSync({ conflictId, noteId, strategy, resolvedContent }) {
  // 逻辑见上方 3.6 节
  // ...
}

// 5. 新增：获取所有未解决冲突
async getUnresolvedConflicts(kbGuid) {
  return DatabaseClient.conflict.getUnresolved(kbGuid)
}

// 6. 新增：标记冲突已读（从"同步按钮红点"中消除）
async markConflictAsRead(conflictId) {
  // 可选：前端用 localStorage 记录已显示过的冲突
  // 后端不需要改动，因为 conflict_log.resolved=0 才是核心状态
}
```

##### 4.3 UI 事件监听增强

```javascript
// CloudSyncService.addListener 接收的新事件
addListener(callback) {
  this._listeners.push(callback)
  // 自动分发已存在的冲突到新监听者
  if (this._status.conflicts > 0) {
    callback({ type: 'conflict_pending', count: this._status.conflicts })
  }
}
```

---

#### 五、diffUtils.js 详细设计

##### 5.1 技术选型

选用 `diff-match-patch`（Google 开源，成熟稳定），原因：
- 支持字符级、词级、行级三种 diff 模式
- 语义化 patch，能生成可阅读的差异描述
- 体积小（< 50KB），无外部依赖
- 支持多语言（中文按字符 diff）

**安装**：`yarn add diff-match-patch`（devDependency）

##### 5.2 导出函数

```javascript
// src/utils/diffUtils.js

import DiffMatchPatch from 'diff-match-patch'

const dmp = new DiffMatchPatch()

/**
 * 生成两段文本的行级差异
 * @param {string} oldText - 旧文本（本地版本）
 * @param {string} newText - 新文本（云端版本）
 * @returns {DiffResult} 差异结果
 */
export function computeLineDiff(oldText, newText) {
  const diffs = dmp.diff_main(oldText, newText)
  dmp.diff_cleanupSemantic(diffs)  // 合并相邻的同类变化，提升可读性

  const result = {
    hasDiff: diffs.some(([op]) => op !== 0),
    segments: [],   // 差异段列表
    addedCount: 0,
    removedCount: 0
  }

  for (const [op, text] of diffs) {
    if (op === 1) {
      result.segments.push({ type: 'added', text })
      result.addedCount++
    } else if (op === -1) {
      result.segments.push({ type: 'removed', text })
      result.removedCount++
    } else {
      result.segments.push({ type: 'unchanged', text })
    }
  }

  return result
}

/**
 * 生成可展示的三栏 Diff 视图数据
 * @param {string} localContent - 本地版本
 * @param {string} serverContent - 云端版本
 * @returns {ThreeColumnDiff}
 */
export function computeThreeColumnDiff(localContent, serverContent) {
  const localLines = (localContent || '').split('\n')
  const serverLines = (serverContent || '').split('\n')

  // 行级 LCS diff（更精确的行对比）
  const lineText1 = localLines.join('\n')
  const lineText2 = serverLines.join('\n')
  const diffs = dmp.diff_main(lineText1, lineText2)
  dmp.diff_cleanupSemantic(diffs)

  // 构建三栏数据
  const localLines_out = []
  const serverLines_out = []
  const mergedLines_out = []

  for (const [op, text] of diffs) {
    const lines = text.split('\n')
    if (op === 0) {
      // 未变化：两边都加相同内容
      for (const line of lines) {
        localLines_out.push({ type: 'unchanged', text: line })
        serverLines_out.push({ type: 'unchanged', text: line })
        mergedLines_out.push({ type: 'unchanged', text: line })
      }
    } else if (op === -1) {
      // 本地有，云端删除了
      for (const line of lines) {
        localLines_out.push({ type: 'removed', text: line })
        mergedLines_out.push({ type: 'local_only', text: line })
      }
    } else if (op === 1) {
      // 云端新增，本地没有
      for (const line of lines) {
        serverLines_out.push({ type: 'added', text: line })
        mergedLines_out.push({ type: 'server_only', text: line })
      }
    }
  }

  return {
    hasDiff: diffs.some(([op]) => op !== 0),
    local: localLines_out,
    server: serverLines_out,
    merged: mergedLines_out,
    // 冲突区域标记：找出被双方都修改过的行
    conflictRanges: findConflictRanges(localLines_out, serverLines_out)
  }
}

/**
 * 找出双方都修改过的行（冲突区域）
 * 行号对齐后，同一行在 local 为 removed、在 server 为 added → 冲突
 */
function findConflictRanges(localLines, serverLines) {
  const conflicts = []
  let i = 0, j = 0

  while (i < localLines.length && j < serverLines.length) {
    if (localLines[i].type === 'unchanged' && serverLines[j].type === 'unchanged') {
      i++, j++
    } else if (localLines[i].type === 'removed' && serverLines[j].type === 'added') {
      // 潜在冲突区域
      conflicts.push({ localLine: i, serverLine: j })
      i++, j++
    } else if (localLines[i].type === 'removed') {
      i++
    } else if (serverLines[j].type === 'added') {
      j++
    } else {
      i++, j++
    }
  }

  return conflicts
}

/**
 * 将手动合并后的内容与原始行对齐（用于高亮）
 */
export function alignMergedContent(localContent, serverContent, mergedContent) {
  return computeThreeColumnDiff(localContent, serverContent)
}

/**
 * 生成 diff 统计摘要
 */
export function getDiffStats(localContent, serverContent) {
  const { addedCount, removedCount } = computeLineDiff(localContent, serverContent)
  return {
    linesAdded: addedCount,
    linesRemoved: removedCount,
    hasConflict: addedCount > 0 && removedCount > 0
  }
}
```

##### 5.3 类型定义（TypeScript 说明）

```typescript
// DiffResult
{
  hasDiff: boolean
  segments: Array<{ type: 'added' | 'removed' | 'unchanged'; text: string }>
  addedCount: number
  removedCount: number
}

// ThreeColumnDiff
{
  hasDiff: boolean
  local: Array<{ type: string; text: string }>
  server: Array<{ type: string; text: string }>
  merged: Array<{ type: string; text: string }>
  conflictRanges: Array<{ localLine: number; serverLine: number }>
}
```

---

#### 六、ConflictResolveDialog.vue 组件详细设计

##### 6.1 组件职责

- **接收**：冲突 ID（`conflictId`）或笔记 ID（`noteId`）
- **获取**：通过 `DatabaseClient.conflict.get(conflictId)` 拉取双方快照
- **展示**：三栏 Diff 视图（本地 / 云端 / 合并结果）
- **操作**：提供"以本地为准"、"以云端为准"、"手动合并"三种策略
- **输出**：将用户决策通过事件上报给父组件

##### 6.2 与父组件的交互模式

参照 `OfflineSyncPromptDialog.vue` 的模式，使用 `show()` / `hide()` 实例方法：

```javascript
// 父组件（CloudSyncPanel.vue 或类似组件）
<ConflictResolveDialog ref="conflictDialog" @resolved="onConflictResolved" />

// 打开冲突处理面板
this.$refs.conflictDialog.show({ conflictId, noteId })

// 处理冲突解决事件
async onConflictResolved({ conflictId, noteId, strategy, resolvedContent }) {
  await CloudSyncService.resolveConflictAndSync({
    conflictId, noteId, strategy, resolvedContent
  })
}
```

##### 6.3 组件模板结构

```vue
<template>
  <q-dialog v-model="dialogVisible" persistent>
    <q-card class="conflict-dialog">
      <!-- 头部 -->
      <q-card-section class="conflict-header">
        <q-icon name="warning_amber" size="24px" color="warning" />
        <span class="conflict-title">{{ $t('conflict.title') }}</span>
        <span class="conflict-subtitle">{{ conflict.noteTitle }}</span>
        <q-space />
        <q-btn flat round dense icon="close" @click="handleClose" />
      </q-card-section>

      <!-- 工具栏 -->
      <q-card-section class="conflict-toolbar">
        <q-btn-toggle
          v-model="activeStrategy"
          toggle-color="primary"
          :options="[
            { label: $t('conflict.localWins'), value: 'local_wins' },
            { label: $t('conflict.serverWins'), value: 'server_wins' },
            { label: $t('conflict.manualMerge'), value: 'manual_merge' }
          ]"
          @input="onStrategyChange"
        />
        <q-space />
        <!-- 差异统计 -->
        <span class="diff-stats">
          <span class="text-positive">+{{ diffStats.linesAdded }}</span>
          <span class="text-negative">-{{ diffStats.linesRemoved }}</span>
        </span>
      </q-card-section>

      <!-- 三栏 Diff 视图 -->
      <q-card-section class="conflict-diff-area">
        <div class="diff-columns">
          <!-- 本地版本 -->
          <div class="diff-column local">
            <div class="column-header">
              <q-icon name="computer" />
              <span>{{ $t('conflict.localVersion') }}</span>
              <span class="column-time">{{ formatTime(conflict.local_modified) }}</span>
            </div>
            <div class="diff-content" v-html="localDiffHtml" />
          </div>

          <!-- 云端版本 -->
          <div class="diff-column server">
            <div class="column-header">
              <q-icon name="cloud" />
              <span>{{ $t('conflict.serverVersion') }}</span>
              <span class="column-time">{{ formatTime(conflict.server_modified) }}</span>
            </div>
            <div class="diff-content" v-html="serverDiffHtml" />
          </div>

          <!-- 合并结果 -->
          <div class="diff-column merged">
            <div class="column-header">
              <q-icon name="merge_type" />
              <span>{{ $t('conflict.mergedResult') }}</span>
            </div>
            <div
              class="diff-content merged-editor"
              :contenteditable="activeStrategy === 'manual_merge'"
              @input="onMergedInput"
              v-html="mergedDiffHtml"
            />
          </div>
        </div>
      </q-card-section>

      <!-- 操作按钮 -->
      <q-card-actions align="right" class="conflict-actions">
        <q-btn
          flat
          :label="$t('conflict.cancel')"
          @click="handleClose"
          :disable="isSubmitting"
        />
        <q-btn
          unelevated
          color="primary"
          :label="$t('conflict.confirm')"
          @click="handleConfirm"
          :loading="isSubmitting"
        />
      </q-card-actions>
    </q-card>
  </q-dialog>
</template>
```

##### 6.4 组件样式（SCSS）

```scss
.conflict-dialog {
  width: 1100px;
  max-width: 95vw;
  max-height: 85vh;
  display: flex;
  flex-direction: column;
}

.conflict-header {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 12px 20px;
  border-bottom: 1px solid var(--borderColor);
  background: linear-gradient(135deg, #fff3e0, #ffe0b2);
}

.conflict-title {
  font-size: 16px;
  font-weight: 600;
}

.conflict-subtitle {
  font-size: 13px;
  color: var(--secondaryTextColor);
  max-width: 300px;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.conflict-toolbar {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 8px 20px;
  border-bottom: 1px solid var(--borderColor);
}

.diff-columns {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: 12px;
  height: 450px;
}

.diff-column {
  display: flex;
  flex-direction: column;
  border: 1px solid var(--borderColor);
  border-radius: 6px;
  overflow: hidden;
}

.column-header {
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 8px 12px;
  font-size: 13px;
  font-weight: 500;
  border-bottom: 1px solid var(--borderColor);
}

.local .column-header { background: #ffebee; }
.server .column-header { background: #e3f2fd; }
.merged .column-header { background: #e8f5e9; }

.column-time {
  font-size: 11px;
  color: var(--secondaryTextColor);
  margin-left: auto;
}

.diff-content {
  flex: 1;
  overflow-y: auto;
  padding: 8px 12px;
  font-family: 'JetBrains Mono', 'Fira Code', monospace;
  font-size: 12px;
  line-height: 1.6;
  white-space: pre-wrap;
  word-break: break-all;

  // Diff 高亮样式
  .diff-added {
    background: #c8e6c9;
    color: #2e7d32;
  }
  .diff-removed {
    background: #ffcdd2;
    color: #c62828;
    text-decoration: line-through;
  }
  .diff-conflict {
    background: #fff9c4;
    border-bottom: 2px solid #f9a825;
  }
}

.merged-editor {
  background: #fafafa;
  outline: none;
  cursor: text;

  &[contenteditable='true'] {
    border: 2px solid var(--primaryColor);
    background: #fff;
  }
}

.diff-stats {
  font-size: 13px;
  font-weight: 600;
}
```

##### 6.5 组件 Script

```javascript
// ConflictResolveDialog.vue

import { computeThreeColumnDiff, getDiffStats } from '../../utils/diffUtils'

export default {
  data() {
    return {
      dialogVisible: false,
      conflict: null,        // { local_content, server_content, local_modified, ... }
      noteId: null,
      conflictId: null,
      activeStrategy: 'local_wins',  // 默认以本地为准
      mergedContent: '',
      isSubmitting: false,
      diffData: null,
      diffStats: { linesAdded: 0, linesRemoved: 0 }
    }
  },

  computed: {
    localDiffHtml() { return this.renderDiffHtml(this.diffData?.local || []) },
    serverDiffHtml() { return this.renderDiffHtml(this.diffData?.server || []) },
    mergedDiffHtml() { return this.renderMergedHtml() }
  },

  methods: {
    async show({ conflictId, noteId }) {
      this.dialogVisible = true
      this.isSubmitting = false

      // 加载冲突数据
      this.conflictId = conflictId
      this.noteId = noteId
      this.conflict = await DatabaseClient.conflict.get(conflictId)

      // 计算差异
      this.computeDiff()
    },

    hide() {
      this.dialogVisible = false
      this.conflict = null
    },

    computeDiff() {
      const { localContent, serverContent } = this.conflict
      this.diffData = computeThreeColumnDiff(localContent, serverContent)
      this.diffStats = getDiffStats(localContent, serverContent)
      this.applyDefaultMergedContent()
    },

    applyDefaultMergedContent() {
      // 默认以本地为准：merged = 本地
      if (this.activeStrategy === 'local_wins') {
        this.mergedContent = this.conflict.local_content
      } else if (this.activeStrategy === 'server_wins') {
        this.mergedContent = this.conflict.server_content
      }
      // manual_merge 时保持现有内容
    },

    onMergedInput(event) {
      this.mergedContent = event.target.innerText
      this.activeStrategy = 'manual_merge'  // 手动编辑后自动切换到手动合并模式
    },

    onStrategyChange(strategy) {
      this.activeStrategy = strategy
      this.applyDefaultMergedContent()
    },

    async handleConfirm() {
      this.isSubmitting = true
      try {
        const strategy = this.activeStrategy
        const resolvedContent = strategy === 'manual_merge'
          ? this.mergedContent
          : null

        this.$emit('resolved', {
          conflictId: this.conflictId,
          noteId: this.noteId,
          strategy,
          resolvedContent
        })

        this.hide()
      } finally {
        this.isSubmitting = false
      }
    },

    handleClose() {
      // 关闭不解决冲突，只是隐藏对话框
      // 冲突状态保留在 conflict_log 中
      this.hide()
    },

    renderDiffHtml(lines) {
      // 将行级 diff 数组转为带样式的 HTML
      return lines.map(line => {
        const escaped = this.escapeHtml(line.text)
        if (line.type === 'removed') {
          return `<span class="diff-removed">${escaped}</span>`
        } else if (line.type === 'added') {
          return `<span class="diff-added">${escaped}</span>`
        } else if (line.type === 'local_only') {
          return `<span class="diff-removed">${escaped}</span>`
        } else if (line.type === 'server_only') {
          return `<span class="diff-added">${escaped}</span>`
        }
        return escaped
      }).join('\n')
    },

    renderMergedHtml() {
      const lines = this.diffData?.merged || []
      return lines.map(line => {
        const escaped = this.escapeHtml(line.text)
        if (line.type === 'local_only') {
          return `<span class="diff-conflict">${escaped}</span>`
        } else if (line.type === 'server_only') {
          return `<span class="diff-conflict">${escaped}</span>`
        }
        return escaped
      }).join('\n')
    },

    formatTime(timestamp) {
      if (!timestamp) return ''
      return new Date(timestamp).toLocaleString()
    },

    escapeHtml(text) {
      return text
        .replace(/&/g, '&amp;')
        .replace(/</g, '&lt;')
        .replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;')
    }
  }
}
```

##### 6.6 与父组件的集成

参照 `OfflineSyncPromptDialog.vue` 的事件总线模式，在全局事件总线或 store 中监听冲突：

```javascript
// CloudSyncService.js - 冲突通知处理
SyncService.addListener((event) => {
  if (event.type === 'conflict_detected') {
    // 1. 更新状态
    this._status.conflicts = (this._status.conflicts || 0) + 1
    this._notify({ type: 'conflict_pending', count: this._status.conflicts })

    // 2. 弹出冲突处理对话框
    // 方式 A：直接引用（需在组件树顶层注册 ConflictResolveDialog）
    eventBus.$emit('show-conflict-dialog', {
      conflictId: event.conflictId,
      noteId: event.noteId,
      noteTitle: event.noteTitle
    })

    // 方式 B：如果是单例对话框，可在 App.vue 中全局监听
  }
})
```

---

#### 七、i18n 字符串清单

##### 7.1 新增字符串

**`src/i18n/zh-cn/other.js`** — 在 `syncPullConfirmOk` 之后追加：

```javascript
// ===== 冲突处理 =====
conflict: {
  title: '发现同步冲突',
  titleSingular: '笔记存在同步冲突',
  localVersion: '本地版本',
  serverVersion: '云端版本',
  mergedResult: '合并结果',
  localWins: '以本地为准',
  serverWins: '以云端为准',
  manualMerge: '手动合并',
  localWinsHint: '保留本地修改，云端版本将被覆盖',
  serverWinsHint: '下载云端版本，本地修改将被覆盖',
  manualMergeHint: '在下方的合并结果中手动编辑',
  conflictDetected: '检测到冲突',
  conflictArea: '冲突区域',
  resolveAndSync: '解决冲突并同步',
  skip: '稍后处理',
  confirm: '确认',
  cancel: '取消',
  conflictNote: '该笔记存在未解决的同步冲突',
  conflictCount: '{count} 个冲突',
  conflictCountSingular: '1 个冲突',
  noConflict: '暂无冲突',
  localModified: '本地修改',
  serverModified: '云端修改',
  merged: '已合并',
  resolved: '已解决',
  strategy: {
    local_wins: '以本地为准',
    server_wins: '以云端为准',
    merged: '手动合并'
  },
  syncAfterResolve: '冲突已解决，正在同步...',
  resolveFailed: '解决冲突失败'
}
```

**`src/i18n/en-us/other.js`** — 对应英文翻译：

```javascript
// ===== Conflict Resolution =====
conflict: {
  title: 'Sync Conflict Detected',
  titleSingular: 'Note has a sync conflict',
  localVersion: 'Local Version',
  serverVersion: 'Cloud Version',
  mergedResult: 'Merged Result',
  localWins: 'Keep Local',
  serverWins: 'Keep Cloud',
  manualMerge: 'Manual Merge',
  localWinsHint: 'Keep local changes, cloud version will be overwritten',
  serverWinsHint: 'Download cloud version, local changes will be overwritten',
  manualMergeHint: 'Manually edit in the merged result below',
  conflictDetected: 'Conflict detected',
  conflictArea: 'Conflict area',
  resolveAndSync: 'Resolve & Sync',
  skip: 'Later',
  confirm: 'Confirm',
  cancel: 'Cancel',
  conflictNote: 'This note has an unresolved sync conflict',
  conflictCount: '{count} conflicts',
  conflictCountSingular: '1 conflict',
  noConflict: 'No conflicts',
  localModified: 'Local modified',
  serverModified: 'Cloud modified',
  merged: 'Merged',
  resolved: 'Resolved',
  strategy: {
    local_wins: 'Keep Local',
    server_wins: 'Keep Cloud',
    merged: 'Manual Merge'
  },
  syncAfterResolve: 'Conflict resolved, syncing...',
  resolveFailed: 'Failed to resolve conflict'
}
```

##### 7.2 现有字符串需要修改的部分

| 现有 key | 现有中文 | 修改建议 |
|----------|----------|----------|
| `syncFailed` | 同步失败 | 无需修改，但在 `conflictDetected` 场景下需区分"网络错误"和"冲突" |

---

#### 八、文件改动清单与验收标准

##### 8.1 文件改动总表

| 文件 | 操作 | 改动量 | 说明 |
|------|------|--------|------|
| `src-electron/main-process/electron-main.js` | 改动 | ~80 行 | `initSchema()` 新增 conflict_log 表 + notes 列；新增 8 个 IPC handlers |
| `src/services/SyncService.js` | 改动 | ~50 行 | `pushToCloud()` 中增加冲突检测逻辑；扩展 stats 对象 |
| `src/services/CloudSyncService.js` | 改动 | ~30 行 | 扩展 _status；新增 resolveConflictAndSync 方法；监听 conflict_detected 事件 |
| `src/components/ui/dialog/ConflictResolveDialog.vue` | 新建 | ~250 行 | 三栏 Diff 冲突处理对话框 |
| `src/utils/diffUtils.js` | 新建 | ~120 行 | diff-match-patch 封装工具函数 |
| `src/utils/DatabaseClient.js` | 改动 | ~30 行 | 新增 `DatabaseClient.conflict` 接口（映射到 IPC handlers） |
| `src/i18n/zh-cn/other.js` | 改动 | ~30 行 | 新增冲突相关 i18n 字符串 |
| `src/i18n/en-us/other.js` | 改动 | ~30 行 | 新增冲突相关 i18n 字符串 |

##### 8.2 DatabaseClient.conflict 接口设计

```javascript
// src/utils/DatabaseClient.js — 新增 conflict 模块

DatabaseClient.conflict = {
  async create(params) {
    return ipcRenderer.invoke('db:createConflictLog', params)
  },

  async get(conflictId) {
    return ipcRenderer.invoke('db:getConflictLog', { conflictId })
  },

  async getUnresolved(kbGuid) {
    return ipcRenderer.invoke('db:getUnresolvedConflicts', kbGuid)
  },

  async resolve(conflictId, strategy, resolvedContent) {
    return ipcRenderer.invoke('db:resolveConflict', { conflictId, strategy, resolvedContent })
  },

  async resolveBatch(conflictIds, strategy, resolvedContent) {
    return ipcRenderer.invoke('db:resolveConflictsBatch', { conflictIds, strategy, resolvedContent })
  },

  async getStats(kbGuid) {
    return ipcRenderer.invoke('db:getConflictStats', kbGuid)
  },

  async getByNoteId(noteId) {
    return ipcRenderer.invoke('db:getConflictNote', { noteId })
  }
}
```

##### 8.3 验收标准

###### 功能验收

- [ ] 冲突笔记触发同步时，`conflict_log` 正确记录双方快照，`notes.has_conflict = 1`
- [ ] `ConflictResolveDialog.vue` 三栏 Diff 正确展示本地/云端差异，行级高亮清晰
- [ ] 用户选择"以本地为准" → `conflict_log.resolved_strategy = 'local_wins'` → 笔记内容不变 → 重新同步成功
- [ ] 用户选择"以云端为准" → `conflict_log.resolved_strategy = 'server_wins'` → 本地内容被云端覆盖 → 重新同步成功
- [ ] 用户选择"手动合并" → 用户编辑合并结果 → `conflict_log.resolved_strategy = 'merged'` → 合并内容保存并同步
- [ ] 冲突解决后，`conflict_log.resolved = 1`，笔记 `has_conflict = 0`，`conflict_id = NULL`
- [ ] 非冲突笔记（只有一个版本更新）行为与 Phase 1 之前完全一致（无回归）

###### UI 验收

- [ ] 同步按钮旁显示未解决冲突数量红点（当 `stats.conflicts > 0` 时）
- [ ] 冲突对话框的三栏 Diff 中，删除行显示红色高亮，新增行显示绿色高亮
- [ ] 冲突区域（双方均修改的行）在合并结果中高亮显示
- [ ] 切换"以本地为准/以云端为准/手动合并"时，合并结果实时更新

###### 数据验收

- [ ] 同一笔记多次触发冲突（解决后又修改），生成新的 `conflict_log` 记录
- [ ] 删除笔记时，`conflict_log` 中对应记录级联删除（`ON DELETE CASCADE`）
- [ ] 数据库迁移对已有用户数据无影响（`CREATE TABLE IF NOT EXISTS` + `ALTER TABLE IF NOT EXISTS` 逻辑）

---

### Phase 2 详细实施计划（2-4 周）

#### 目标

引入 Yjs CRDT 层，让冲突合并更智能（自动合并非冲突区域），同时为明年协同办公奠定基础。

**前置说明**：Muya v0.1.2 目前无官方 Yjs 集成插件。因此 Phase 2 采用 **"Yjs 包裹 Muya"** 的混合架构，而非深度替换。这种方式的 CRDT 合并能力会有部分局限，但引入成本最低，且为 Phase 3 深度集成预留了清晰路径。

---

#### 一、安装依赖

```bash
yarn add yjs y-indexeddb y-websocket
yarn add --dev @types/diff-match-patch
```

> **注意**：`y-websocket` 目前不启用（Phase 3 才需要），但先装上方便后续扩展。

---

#### 二、架构设计（详细）

##### 2.1 双层内容架构

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                         渲染进程（Renderer）                                   │
│                                                                               │
│   用户在 Muya 中编辑                                                          │
│        ↓                                                                     │
│   Muya 'change' 事件（debounced 300ms）                                       │
│        ↓                                                                     │
│   yjsBridge.onMuyaChange(markdown)                                            │
│        ↓                                                                     │
│   Y.Doc.getText('content').applyDelta([{ insert: newMarkdown }])              │
│        ↓                                                                     │
│   Y.Doc 触发 obs，观察者链：                                                  │
│        ├── y-indexeddb（自动持久化到 IndexedDB）                              │
│        ├── SQLite（通过 IPC 同步 content_yjs 二进制）                         │
│        └── SyncService（通过 IPC 推送 Yjs Update 到云端）                       │
│                                                                               │
│   打开笔记时：                                                                │
│   Y.Doc 已有数据 ← y-indexeddb（离线优先）                                   │
│        ↓（无 IndexedDB 数据）                                                │
│   ← notes.content_yjs（SQLite）                                               │
│        ↓（无 SQLite）                                                         │
│   ← notes.content（纯文本，兼容性降级）                                       │
│        ↓                                                                     │
│   Y.Doc → Markdown → Muya.setMarkdown()                                       │
└──────────────────────────────────────────────────────────────────────────────┘
```

**核心设计原则**：
- **Y.Doc 是内存中的编辑权威**：用户所有编辑都通过 Y.Doc 反映
- **SQLite 的 `notes.content` 保持不变**：用于导出、兼容、其他模块读取
- **`notes.content_yjs`**：存储 Y.Doc 的序列化二进制（用于 Phase 2 的同步和冲突合并）
- **双写保证一致性**：每次 Y.Doc 变化时，同时更新 `content`（纯文本）和 `content_yjs`（二进制）

##### 2.2 数据流向图（完整版）

```
编辑流程（正向）:
Muya change event
    ↓
yjsBridge.syncFromMuya(markdown)
    ↓
Y.Doc.getText('content').applyDelta()
    ↓
┌─────────────────────────────────────────┐
│  Yjs Observer 链（并行触发）              │
│                                         │
│  1. y-indexeddb Provider                │
│     → 自动持久化到 IndexedDB             │
│                                         │
│  2. SQLite IPC Bridge                   │
│     → ipcRenderer.invoke('db:saveYjsState')
│     → 更新 notes.content + content_yjs   │
│                                         │
│  3. SyncService IPC Bridge              │
│     → ipcRenderer.invoke('sync:pushYjsUpdate')
│     → 推送 Yjs Update 到云端             │
└─────────────────────────────────────────┘

打开笔记流程（反向）:
y-indexeddb Provider
    ↓（有数据）
Y.Doc
    ↓
Y.Doc.getText('content').toString()
    ↓
Muya.setMarkdown(markdown)
    ↓
用户看到笔记内容

y-indexeddb Provider
    ↓（无数据）
SQLite: notes.content_yjs
    ↓（无数据）
SQLite: notes.content（降级）
    ↓
Y.Doc = Y.Doc.create() → initFromMarkdown(content)
    ↓
Muya.setMarkdown(markdown)
```

---

#### 三、yjsBridge.js 核心设计

##### 3.1 模块职责

新建 `src/utils/yjsBridge.js`，作为 Yjs 文档的生命周期管理器。它负责：
- Y.Doc 的创建、销毁、按笔记隔离
- Muya ↔ Y.Doc 的双向同步
- y-indexeddb Provider 的挂载/卸载
- Y.Doc 与 SQLite 的持久化同步
- Yjs Update 的生成与消费（用于云端同步）

##### 3.2 核心类设计

```javascript
// src/utils/yjsBridge.js

import * as Y from 'yjs'
import { IndexeddbPersistence } from 'y-indexeddb'

class YjsBridge {
  constructor() {
    // 按 docGuid 缓存 Y.Doc 实例，避免重复创建
    this._docs = new Map()           // docGuid → { doc, obs, provider }
    this._pendingSave = null          // debounced SQLite 写任务
    this._pendingSaveTimer = null

    // 注册全局 Yjs Observer（在 bridge 实例化时注册一次）
    this._globalObserver = this._onYjsChange.bind(this)
  }

  // ══════════════════════════════════════════════════════
  // 公开 API：供 Muya.vue 调用
  // ══════════════════════════════════════════════════════

  /**
   * 打开笔记：获取或创建 Y.Doc，加载内容，返回 Markdown 字符串
   * @param {string} docGuid - 笔记 docGuid
   * @param {string} initialContent - 原始内容（用于初始化）
   * @returns {Promise<string>} - 当前 Y.Doc 的 Markdown 内容
   */
  async openNote(docGuid, initialContent) {
    // 1. 获取或创建 Y.Doc
    const docEntry = this._getOrCreateDoc(docGuid)

    // 2. 如果是新建 Doc（无内容），用 initialContent 初始化
    if (docEntry.isNew) {
      const yText = docEntry.doc.getText('content')
      if (initialContent && yText.length === 0) {
        docEntry.doc.transact(() => {
          yText.insert(0, initialContent)
        })
      }
      docEntry.isNew = false
    }

    // 3. y-indexeddb 已自动加载数据（Provider 在 _getOrCreateDoc 时挂载）
    // 4. 返回当前内容
    return docEntry.doc.getText('content').toString()
  }

  /**
   * 关闭笔记：卸载 y-indexeddb Provider，释放内存
   * @param {string} docGuid
   */
  closeNote(docGuid) {
    const entry = this._docs.get(docGuid)
    if (!entry) return

    // 触发最后一次持久化
    this._persistToSQLite(docGuid)

    // 卸载 Provider
    if (entry.provider) {
      entry.provider.destroy()
    }

    // 移除 Observer
    if (entry.unobserve) {
      entry.unobserve()
    }

    this._docs.delete(docGuid)
    console.log(`[yjsBridge] Closed doc: ${docGuid}`)
  }

  /**
   * 从 Muya 同步内容到 Y.Doc（用户在 Muya 中编辑时调用）
   * @param {string} docGuid
   * @param {string} markdown - 当前 Muya 的完整 Markdown 内容
   */
  syncFromMuya(docGuid, markdown) {
    const entry = this._docs.get(docGuid)
    if (!entry) return

    const yText = entry.doc.getText('content')
    const currentContent = yText.toString()

    if (markdown === currentContent) return

    // 使用 transact 保证原子性
    entry.doc.transact(() => {
      yText.delete(0, yText.length)
      yText.insert(0, markdown)
    }, 'muya-sync')
  }

  /**
   * 获取当前笔记的 Yjs Update（二进制），用于推送到云端
   * @param {string} docGuid
   * @returns {Uint8Array | null}
   */
  getYjsUpdate(docGuid) {
    const entry = this._docs.get(docGuid)
    if (!entry) return null
    return Y.encodeStateAsUpdate(entry.doc)
  }

  /**
   * 获取两个笔记之间的增量 Update（用于 Delta Sync）
   * @param {string} docGuid
   * @param {Uint8Array} baseState - 基准状态（上次同步时的 state vector）
   * @returns {Uint8Array} - 增量 Update
   */
  getYjsUpdateSince(docGuid, baseState) {
    const entry = this._docs.get(docGuid)
    if (!entry) return new Uint8Array(0)
    return Y.encodeStateAsUpdateFromVector(entry.doc, baseState)
  }

  /**
   * 将云端拉取的 Yjs Update 合入本地 Doc
   * @param {string} docGuid
   * @param {Uint8Array} update - 云端 Yjs Update
   */
  applyRemoteUpdate(docGuid, update) {
    const entry = this._docs.get(docGuid)
    if (!entry) return

    // 使用 transact + 'remote-sync' origin 标记来源
    // 这样可以区分"本地编辑"和"远程更新"
    entry.doc.transact(() => {
      Y.applyUpdate(entry.doc, update)
    }, 'remote-sync')

    // 合入后触发 SQLite 更新
    this._persistToSQLite(docGuid)

    // 返回合并后的 Markdown（供 Muya 刷新显示）
    return entry.doc.getText('content').toString()
  }

  // ══════════════════════════════════════════════════════
  // 私有方法
  // ══════════════════════════════════════════════════════

  _getOrCreateDoc(docGuid) {
    if (this._docs.has(docGuid)) {
      return this._docs.get(docGuid)
    }

    const doc = new Y.Doc()
    const provider = new IndexeddbPersistence(`memocast-${docGuid}`, doc)

    // Provider 就绪后（IndexedDB 加载完成），Provider 会自动把数据填入 doc
    provider.on('synced', () => {
      console.log(`[yjsBridge] IndexedDB synced for: ${docGuid}`)
      // 数据已从 IndexedDB 加载到 doc，无需额外处理
    })

    // 注册 Observer（监听 doc 变化，触发持久化和同步）
    const yText = doc.getText('content')
    const unobserve = yText.observe(this._globalObserver, docGuid)

    const entry = {
      doc,
      provider,
      unobserve,
      isNew: yText.length === 0   // 无内容 = 新建
    }

    this._docs.set(docGuid, entry)
    console.log(`[yjsBridge] Created new Y.Doc: ${docGuid}`)

    return entry
  }

  /**
   * 全局 Yjs Observer：监听所有 doc 的变化
   * 由各个 Y.Text 的 observe 回调统一路由到这里
   */
  _onYjsChange(events, docGuid) {
    for (const event of events) {
      // event.origin 标识变更来源
      // 'muya-sync' = 用户在 Muya 中编辑产生
      // 'remote-sync' = 远程更新合入产生
      // null = y-indexeddb 自动同步（忽略，避免循环）

      if (event.transaction.origin === null) {
        // IndexedDB 自动同步，不需要处理
        return
      }

      // 更新 SQLite（debounced，避免频繁写入）
      this._schedulePersistToSQLite(docGuid)
    }
  }

  _schedulePersistToSQLite(docGuid) {
    if (this._pendingSaveTimer) {
      clearTimeout(this._pendingSaveTimer)
    }
    this._pendingSaveTimer = setTimeout(() => {
      this._persistToSQLite(docGuid)
    }, 1000)  // 1秒防抖（用户连续输入时只每1秒写一次 SQLite）
  }

  async _persistToSQLite(docGuid) {
    const entry = this._docs.get(docGuid)
    if (!entry) return

    const content = entry.doc.getText('content').toString()
    const yjsState = Y.encodeStateAsUpdate(entry.doc)  // 完整二进制
    const noteId = await this._getNoteIdByDocGuid(docGuid)

    if (!noteId) return

    // 同时更新 content（纯文本）和 content_yjs（二进制）
    // isSystemUpdate = true 表示这是系统级更新，不触发 dirty=1
    await DatabaseClient.notes.update(noteId, {
      content,                    // 保持纯文本（兼容）
      content_yjs: yjsState,     // Y.Doc 序列化（用于云端同步）
      local_modified: Date.now() // 更新时间戳（dirty=1 已在 save 流程处理）
    }, { isSystemUpdate: true })

    console.log(`[yjsBridge] Persisted to SQLite: ${docGuid}, content length: ${content.length}`)
  }

  async _getNoteIdByDocGuid(docGuid) {
    // 缓存 noteId 避免重复查询
    if (this._noteIdCache && this._noteIdCache.docGuid === docGuid) {
      return this._noteIdCache.noteId
    }
    const note = await DatabaseClient.notes.getByDocGuid(docGuid)
    if (note) {
      this._noteIdCache = { docGuid, noteId: note.id }
      return note.id
    }
    return null
  }
}

// 单例导出
export const yjsBridge = new YjsBridge()
```

##### 3.3 模块导出

```javascript
export async function initYjsBridge() {
  // 延迟实例化（等待 Vue app ready）
}

export function getYjsBridge() {
  return yjsBridge
}
```

---

#### 四、Muya.vue 改动

##### 4.1 改动点概述

在现有 Muya.vue 基础上改动 4 处：

1. **`created()`**：初始化 yjsBridge，实例化
2. **`currentNote` watcher**：改为从 yjsBridge.openNote() 获取内容，而非直接从 store
3. **`change` event handler**：将 Muya 内容同步到 yjsBridge
4. **`captureCurrentContent()`**：改为从 yjsBridge 获取最新内容

##### 4.2 改动详情

```javascript
// src/components/ui/editor/Muya.vue

import { yjsBridge } from '../../utils/yjsBridge'

export default {
  data() {
    return {
      // ... 现有字段
      _yjsDocGuid: null,   // 新增：当前笔记的 docGuid（用于判断是否需要切换 Y.Doc）
    }
  },

  // ══════════════════════════════════════════════════════
  // created() 改动：初始化 yjsBridge
  // ══════════════════════════════════════════════════════
  async created() {
    // ... 现有代码（Muya 实例创建等）...

    // 订阅 yjsBridge 的远程更新通知（当云端拉取到新内容时）
    this._setupYjsRemoteObserver()
  },

  // ══════════════════════════════════════════════════════
  // currentNote watcher 改动：改为从 yjsBridge 获取内容
  // ══════════════════════════════════════════════════════
  watch: {
    async currentNote(currentData) {
      console.log(`\n[Muya watcher] ⚡ FIRED! type: ${typeof currentData}`)

      // 解析笔记内容
      let markdownContent = ''
      let docGuid = null

      if (typeof currentData === 'string') {
        markdownContent = currentData || ''
      } else if (currentData && typeof currentData === 'object') {
        markdownContent = currentData.__markdown || ''
        docGuid = currentData.__docGuid || currentData.info?.docGuid || null
      }

      if (!docGuid) {
        // 无 docGuid（例如草稿笔记），走原有逻辑
        this.contentEditor.setMarkdown(markdownContent)
        return
      }

      // ════════════════════════════════════════════════════
      // Phase 2 改动：使用 yjsBridge 管理笔记内容
      // ════════════════════════════════════════════════════

      // 1. 关闭上一个 Y.Doc（如果有切换笔记）
      if (this._yjsDocGuid && this._yjsDocGuid !== docGuid) {
        yjsBridge.closeNote(this._yjsDocGuid)
      }

      // 2. 打开/创建 Y.Doc，获取当前内容
      //    优先级：y-indexeddb（离线优先）> notes.content > notes.content_yjs > initialContent
      const yjsContent = await yjsBridge.openNote(docGuid, markdownContent)

      // 3. 用 Yjs 内容替换 Muya 显示
      //    如果 y-indexeddb 有数据，yjsContent 是离线编辑结果（可能与 markdownContent 不同）
      this.contentEditor.setMarkdown(yjsContent)
      this._yjsDocGuid = docGuid

      this.firstTimeLoad = true
    }
  },

  methods: {
    // ══════════════════════════════════════════════════════
    // change event handler 改动：同步到 yjsBridge
    // ══════════════════════════════════════════════════════
    _setupEditorChangeHandler() {
      this.contentEditor.on('change', _.debounce(({ markdown: curData }) => {
        // ... 现有逻辑（noteState 判断等）...

        if (curData !== currentNoteContent /* ... 现有条件 ... */) {
          this.updateNoteState('changed')

          // ════════════════════════════════════════════
          // Phase 2 改动：同步到 Y.Doc
          // ════════════════════════════════════════════
          if (this._yjsDocGuid) {
            yjsBridge.syncFromMuya(this._yjsDocGuid, curData)
          }
        }
      }, 300, { leading: true }))
    },

    // ══════════════════════════════════════════════════════
    // captureCurrentContent() 改动：从 yjsBridge 获取最新内容
    // ══════════════════════════════════════════════════════
    captureCurrentContent: function () {
      // 优先从 yjsBridge 获取最新内容（它可能包含还未写入 SQLite 的编辑）
      if (this._yjsDocGuid) {
        const yjsContent = yjsBridge.getDocContent(this._yjsDocGuid)
        if (yjsContent !== undefined) {
          const currentNote = this.$store.state.server.currentNote
          return {
            markdown: yjsContent,
            docGuid: currentNote.info.docGuid,
            title: currentNote.info.title,
            resources: currentNote.resources || [],
            timestamp: Date.now(),
            noteState: this.noteState
          }
        }
      }

      // 降级：走原有逻辑
      return this._captureCurrentContentLegacy()
    },

    // ══════════════════════════════════════════════════════
    // 远程更新回调：当云端有新的 Yjs Update 时触发
    // ══════════════════════════════════════════════════════
    _setupYjsRemoteObserver() {
      // 监听 SyncService 的远程更新事件
      SyncService.addListener((event) => {
        if (event.type === 'remote_update_applied') {
          if (event.docGuid === this._yjsDocGuid) {
            // 获取合并后的内容并刷新编辑器
            const mergedContent = event.mergedMarkdown
            if (mergedContent && this.contentEditor) {
              this.contentEditor.setMarkdown(mergedContent)
            }
          }
        }
      })
    }
  },

  beforeDestroy() {
    // 关闭当前 Y.Doc，释放内存
    if (this._yjsDocGuid) {
      yjsBridge.closeNote(this._yjsDocGuid)
      this._yjsDocGuid = null
    }
  }
}
```

---

#### 五、SQLite schema 扩展

##### 5.1 新增字段

```sql
ALTER TABLE notes ADD COLUMN content_yjs BLOB;
```

> **存储内容**：`Y.encodeStateAsUpdate(doc)` 的完整二进制输出。
> 用途：Phase 2 中，当 y-indexeddb 无数据时，从这里恢复 Y.Doc 状态。
> 注意：用 BLOB 而非 TEXT，避免大文本的编码问题。

##### 5.2 新增 IPC handlers

| Handler | 说明 |
|---------|------|
| `db:saveYjsState` | 保存 `content_yjs`（Y.Doc 序列化）到 SQLite |
| `db:getYjsState` | 读取笔记的 `content_yjs` 二进制 |

```javascript
// electron-main.js

ipcMain.handle('db:saveYjsState', async (event, { noteId, contentYjs }) => {
  execRun(
    `UPDATE notes SET content_yjs = ?, local_modified = ? WHERE id = ?`,
    [contentYjs, Date.now(), noteId]
  )
  return { success: true }
})

ipcMain.handle('db:getYjsState', async (event, { noteId }) => {
  const row = execOne(`SELECT content_yjs FROM notes WHERE id = ?`, [noteId])
  return { contentYjs: row?.content_yjs || null }
})
```

---

#### 六、SyncService 改动（Yjs Update 同步）

##### 6.1 改动思路

Phase 1 中 `pushToCloud` 推送的是笔记原文（Markdown 文本）。Phase 2 改为推送 `Yjs Update` 二进制，增量传输（只传 diff），同时保留纯文本推送作为降级方案。

##### 6.2 双模式推送代码

> **完整代码**见第五节"兼容设计"中的 `SyncService 双模式分支`部分（第 510-573 行），包含 `pushToCloud()` 主函数（基础模式默认 + 增强模式可选）、`_pushYjsUpdate()` 和 `_getCrdtMode()` 辅助函数。

---

##### 6.3 拉取流程改动（Phase 2 增强模式降级路径）

```javascript
// src/services/SyncService.js

async pushToCloud() {
  const kbGuid = getKbGuid()
  const pendingNotes = await DatabaseClient.notes.getPendingByKbGuid(kbGuid)
  const stats = { pushed: 0, conflicts: 0, errors: 0, skipped: 0, backfilled: 0 }

  // 获取是否开启增强模式（Yjs CRDT），默认为 false（基础模式）
  const useCrdt = await this._getCrdtMode(kbGuid)

  for (const note of pendingNotes) {
    const hasCloudGuid = note.doc_guid && !note.doc_guid.startsWith('local_')

    if (hasCloudGuid) {
      // ════════════════════════════════════════════════════════════
      // Phase 2 模式（Yjs CRDT）：优先推送 Yjs Update
      // ════════════════════════════════════════════════════════════
      if (syncMode === 'crdt') {
        ? new Uint8Array(note.last_sync_state_vector)
        : null

      // 计算增量 Update
      let updatePayload = null
      if (note.content_yjs) {
        const yjsUpdate = await yjsBridge.getYjsUpdateSince(note.doc_guid, lastStateVector)
        if (yjsUpdate && yjsUpdate.length > 0) {
          updatePayload = { type: 'yjs_update', data: Array.from(yjsUpdate) }
        }
      }

      // 没有增量 Update（内容未变），跳过推送
      if (!updatePayload) {
        stats.skipped++
        continue
      }

      // 尝试 Yjs Update 推送
      try {
        await api.pushYjsUpdate(note.doc_guid, updatePayload.data, kbGuid)
        // 推送成功后，保存新的 state vector
        const newStateVector = Y.encodeStateVector(yjsBridge.getYDoc(note.doc_guid))
        await DatabaseClient.notes.update(note.id, {
          last_sync_state_vector: Array.from(newStateVector)
        }, { isSystemUpdate: true })
      } catch (err) {
        if (err.code === 'YJS_UPDATE_NOT_SUPPORTED') {
          // 云端不支持 Yjs Update，降级到纯文本推送
          await api.updateDoc(note.doc_guid, {
            title: note.title,
            content: note.content,
            category: note.category,
            tags: note.tags
          }, kbGuid)
        } else {
          throw err
        }
      }

      // 标记 dirty=0（Phase 2 增强模式成功路径）
      await DatabaseClient.notes.update(note.id, {
        dirty: 0,
        server_modified: Date.now()
      }, { isSystemUpdate: true })

      stats.pushed++
    }
    // ... local_ 前缀笔记处理（不变）
  }

  return stats
}
```

##### 6.3 拉取流程改动

    if (localNote) {
      // 笔记已存在：检查是否需要合并
      if (localNote.content_yjs) {
        // 本地有 Yjs 状态：尝试拉取 Yjs Update 并合并
        try {
          const remoteUpdate = await api.pullYjsUpdate(cloudNote.guid, kbGuid)
          if (remoteUpdate) {
            // 将远程 Update 合入本地 Y.Doc
            const mergedContent = yjsBridge.applyRemoteUpdate(
              localNote.doc_guid,
              new Uint8Array(remoteUpdate)
            )

            // 更新本地 SQLite
            await DatabaseClient.notes.update(localNote.id, {
              content: mergedContent,
              // content_yjs 已在 applyRemoteUpdate 中更新
              dirty: 0,
              server_modified: cloudNote.data_modified
            }, { isSystemUpdate: true })

            stats.merged = (stats.merged || 0) + 1
            continue
          }
        } catch (err) {
          console.warn(`[SyncService] Yjs merge failed for ${cloudNote.guid}:`, err)
          // 降级到 Phase 1 的冲突处理
        }
      }

      // 非 Yjs 笔记或 Yjs 合并失败：走 Phase 1 冲突检测
      if (localNote.local_modified > cloudNote.data_modified) {
        // 冲突：本地更新过
        // ... (Phase 1 冲突检测逻辑)
      } else {
        // 云端更新更多：拉取云端
        // ... (Phase 1 restore 逻辑)
      }
    }
  }

  return stats
}
```

##### 6.4 WizNote API 扩展（待协商）

当前 WizNote API 不支持 Yjs Update 二进制，需要在云端扩展字段或自建 Gateway：

```javascript
// WizNote API 扩展（Phase 3 Gateway 方案）
const api = {
  // ... 现有方法 ...

  // Phase 2 可用：扩展字段存储 Yjs Update（需 WizNote 支持）
  async pushYjsUpdate(docGuid, updateData, kbGuid) {
    // 方案 A：在现有 updateNote 扩展字段中传 base64(updateData)
    return this.execRequest('note/update', {
      method: 'PUT',
      docGuid,
      body: {
        yjs_update: btoa(String.fromCharCode(...updateData)),
        yjs_version: 1
      }
    })
  },

  async pullYjsUpdate(docGuid, kbGuid) {
    const info = await this.getNoteInfo(docGuid)
    if (info.yjs_update) {
      const binary = atob(info.yjs_update)
      return Uint8Array.from(binary, c => c.charCodeAt(0))
    }
    return null
  }
}
```

---

#### 七、冲突处理升级（从 Phase 1 到 Phase 2）

##### 7.1 升级对比

| 方面 | Phase 1（基于时间戳） | Phase 2（基于 Yjs） |
|------|----------------------|---------------------|
| **自动合并** | 无，所有冲突都需要用户处理 | 自动合并非冲突区域 |
| **冲突粒度** | 全文 Diff | 精确到操作（如"第5段第3行"） |
| **快速连续编辑** | 可能丢失（< 1s） | 完整保留（Yjs CRDT） |
| **离线合并质量** | 低（只保存时间戳） | 高（保存完整操作历史） |
| **云端存储** | 纯文本 | Yjs Update 二进制 |
| **ConflictResolveDialog** | 全文三栏 Diff | 智能 Diff（仅展示冲突区域） |

##### 7.2 ConflictResolveDialog 升级

Phase 2 的冲突对话框在 Phase 1 基础上做两处升级：

1. **内容获取来源**：改为从 `yjsBridge.applyRemoteUpdate()` 获取合并后的精确差异，而非全文 Diff
2. **冲突区域高亮**：利用 Yjs 的操作信息，只高亮真正冲突的段落

```vue
<!-- ConflictResolveDialog.vue - Phase 2 升级部分 -->

<!-- 工具栏新增：展示冲突区域数 -->
<div class="conflict-summary">
  <span v-if="yjsConflictRanges && yjsConflictRanges.length > 0">
    {{ $t('conflict.conflictAreas', { count: yjsConflictRanges.length }) }}
  </span>
  <span v-else-if="diffStats.hasConflict === false">
    {{ $t('conflict.autoMerged') }}
  </span>
</div>

<!-- 合并结果栏新增：自动合并区域 vs 手动处理区域 -->
<div class="diff-segment auto-merged">
  <!-- Phase 1: 全文 Diff -->
  <!-- Phase 2: 智能 Diff，仅展示需要用户决策的区域 -->
  <div
    v-for="(segment, idx) in smartConflictSegments"
    :key="idx"
    :class="['diff-segment', segment.type]"
  >
    <span v-if="segment.type === 'conflict'" class="conflict-marker">⚠️</span>
    {{ segment.text }}
  </div>
</div>
```

##### 7.3 i18n 新增字符串

```javascript
// zh-cn/other.js - 新增
conflict: {
  // ... Phase 1 字符串 ...

  // Phase 2 新增
  conflictAreas: '{count} 处冲突区域需手动处理',
  autoMerged: '已自动合并所有非冲突内容',
  mergeSkipped: '跳过合并，使用本地版本',
  conflictArea: '冲突区域（需手动处理）',
  autoMergeNote: '已自动合并不同段落的修改，下方仅展示存在冲突的区域'
}
```

---

#### 八、文件改动清单与验收标准

##### 8.1 文件改动总表

| 文件 | 操作 | 改动量 | 说明 |
|------|------|--------|------|
| `package.json` | 改动 | ~3 行 | 新增 `yjs`, `y-indexeddb` 依赖 |
| `src/utils/yjsBridge.js` | 新建 | ~280 行 | Yjs 文档生命周期管理器 |
| `src/utils/DatabaseClient.js` | 改动 | ~10 行 | 新增 `db:saveYjsState`, `db:getYjsState` 接口映射 |
| `src-electron/main-process/electron-main.js` | 改动 | ~30 行 | 新增 IPC handlers + `content_yjs` 列迁移 |
| `src/components/ui/editor/Muya.vue` | 改动 | ~80 行 | 接入 yjsBridge 作为内容中间层 |
| `src/services/SyncService.js` | 改动 | ~100 行 | 改为推送 Yjs Update，增量同步 |
| `src/services/CloudSyncService.js` | 改动 | ~20 行 | 新增 `remote_update_applied` 事件处理 |
| `src/components/ui/dialog/ConflictResolveDialog.vue` | 改动 | ~40 行 | Phase 2 智能冲突 Diff 升级 |

##### 8.2 验收标准

###### 功能验收

- [ ] yjsBridge 实例化后，`openNote(docGuid, content)` 能正确创建/加载 Y.Doc
- [ ] y-indexeddb Provider 自动持久化，关闭应用重开后笔记内容不丢失
- [ ] 用户在 Muya 中编辑，`syncFromMuya(docGuid, markdown)` 正确更新 Y.Doc
- [ ] `getYjsUpdateSince()` 返回正确增量（仅包含上次同步后的变化）
- [ ] `applyRemoteUpdate()` 正确合入远程 Update 并触发 SQLite 更新
- [ ] Muya 切换笔记时，旧 Y.Doc 正确关闭并释放内存（`closeNote()`）

###### 同步验收

- [ ] SyncService.pushToCloud() 优先推送 Yjs Update，增量传输
- [ ] Yjs Update 推送失败时正确降级到纯文本推送（不阻塞同步）
- [ ] 同一笔记在两个设备分别编辑不同段落，重新联网后自动合并，无内容丢失（CRDT 合并）
- [ ] 同一笔记在两个设备编辑同一段落，展示精确到段落的冲突 UI

###### 兼容性验收

- [ ] 旧笔记（无 `content_yjs` 字段）降级到纯文本模式，不报错
- [ ] Phase 1 的 conflict_log 数据与 Phase 2 Yjs 合并流程完全兼容
- [ ] 切换笔记时，`captureCurrentContent()` 从 yjsBridge 获取最新内容（包含未保存的编辑）

---

### Phase 3 详细实施计划（长期）

#### Phase 3 的目标

Phase 1 + Phase 2 已经解决了单用户多设备的本地文件冲突问题。Phase 3 的核心目标是**引入实时协同能力**（多人同时编辑同一文档），需要配套的基础设施升级。

#### 路线一：自建 CRDT Gateway（推荐）

##### 架构设计

```
┌─────────────────────────────────────────────────────────────────┐
│  Memocast Desktop / Mobile / Web                                │
│  (Yjs Client)                                                    │
│      │                                                           │
│      │ y-websocket                                               │
│      ▼                                                           │
│  ┌──────────────────────────────────────────────────────────┐    │
│  │  CRDT Gateway                                             │    │
│  │  (Cloudflare Workers / Vercel Edge / 独立 Node.js 服务)  │    │
│  │                                                            │    │
│  │  ├── WebSocket Server（Hocuspocus 或自定义）               │    │
│  │  ├── Yjs Server（服务端 Y.Doc 实例）                       │    │
│  │  ├── y-leveldb / y-mongodb（Yjs 状态持久化）              │    │
│  │  └── WizNote Adapter（WizNote 存储层）                    │    │
│  └──────────────────────┬─────────────────────────────────────┘    │
│                         │                                         │
│                         ▼                                         │
│              WizNote REST API（存储最终 Markdown）                 │
└─────────────────────────────────────────────────────────────────┘
```

##### Gateway 职责

| 职责 | 说明 |
|------|------|
| **Yjs WebSocket 服务** | 接收客户端的 Yjs Update，维持服务端 Y.Doc |
| **CRDT 合并** | 服务端 Y.Doc 自动合并多客户端的 Update |
| **持久化** | 将合并后的 Y.Doc 定期导出为 Markdown，存储到 WizNote |
| **离线感知** | 检测客户端断线，触发最终合并 |
| **冲突保护** | CRDT 已处理操作级冲突，Gateway 不需要额外冲突处理 |
| **鉴权** | 验证用户 token，过滤非授权 Update |

##### 技术选型

| 组件 | 推荐方案 | 说明 |
|------|----------|------|
| WebSocket 框架 | **Hocuspocus**（Yjs 官方 Server） | 开源，基于 Node.js，与 Yjs 无缝集成 |
| 持久化 | **y-leveldb** 或 **PostgreSQL**（y-postgres） | 持久化 Y.Doc 状态 |
| 部署平台 | **Cloudflare Workers** 或 **独立 Node.js** | Workers 需要 WebSocket 支持（Durable Objects） |
| WizNote 集成 | REST API + 自定义 Adapter | Gateway 导出 Markdown 后写入 WizNote |

##### Hocuspocus Server 示例

```javascript
// gateway/server.js

import { Server } from '@hocuspocus/server'
import { LeveldbPersistence } from 'y-leveldb'
import { WizNoteAdapter } from './wiznote-adapter'

const persistence = new LeveldbPersistence('./yjs-state')
const wizNoteAdapter = new WizNoteAdapter({
  apiBase: process.env.WIZNOTE_API_BASE,
  token: process.env.WIZNOTE_TOKEN
})

const server = Server.configure({
  port: 1234,

  async onConnect({ document, socketId, token }) {
    // 鉴权：验证用户 token
    if (!await validateToken(token)) {
      throw new Error('Unauthorized')
    }
  },

  async onLoadDocument({ document, documentName }) {
    // 从持久化存储加载 Y.Doc
    const persistedYDoc = await persistence.getYDoc(documentName)
    if (persistedYDoc) {
      Y.applyUpdate(document, Y.encodeStateAsUpdate(persistedYDoc))
    }
    return document
  },

  async onChange({ document, documentName, context }) {
    // Y.Doc 变化时：持久化 + 导出 Markdown 到 WizNote
    const yDoc = persistence.getYDoc(documentName) || document
    const markdown = yDoc.getText('content').toString()

    // 异步写入 WizNote（不影响实时同步）
    await wizNoteAdapter.updateNote(documentName, markdown)
    await persistence.storeYDoc(documentName, document)
  }
})

server.listen()
```

##### Gateway 与 Memocast 的客户端集成

```javascript
// src/utils/yjsBridge.js - 新增 WebSocket Provider

import { WebsocketProvider } from 'y-websocket'

class YjsBridge {
  _connectWebSocket(docGuid, token) {
    // Phase 3 新增：WebSocket Provider 替代 HTTP 轮询
    const wsProvider = new WebsocketProvider(
      'wss://gateway.example.com',  // Gateway 地址
      `note:${docGuid}`,            // 文档名称（格式：note:{docGuid}）
      this._docs.get(docGuid).doc,
      { params: { token } }        // 鉴权 token
    )

    wsProvider.on('status', ({ status }) => {
      console.log(`[yjsBridge] WS status: ${status}`)  // 'connected' | 'disconnected'
    })

    wsProvider.on('sync', (isSynced) => {
      if (isSynced) {
        console.log(`[yjsBridge] Synced with server: ${docGuid}`)
      }
    })

    return wsProvider
  }
}
```

---

#### 路线二：扩展 WizNote API

如果 WizNote 团队愿意支持 Yjs 语义，可以直接在 WizNote 服务端实现 CRDT 合并，而不需要自建 Gateway：

```
Memocast (Yjs Client)  →  WizNote API (原生支持 Yjs)  →  WizNote DB (Yjs 状态)
```

**需要 WizNote 提供的 API 扩展**：

| API | 扩展内容 |
|-----|----------|
| `POST /note/yjs-update` | 接收 Yjs Update 二进制，存储到服务端 Y.Doc |
| `GET /note/:guid/yjs-update` | 获取服务端 Y.Doc 的增量 Update |
| `GET /note/:guid/markdown` | 获取合并后的 Markdown 文本（兼容旧客户端） |
| `POST /note/:guid/subscribe` | WebSocket 订阅笔记变更（实时协同） |

---

#### Phase 3 验收标准

###### 协同编辑

- [ ] 多个用户同时编辑同一笔记，各自的光标位置实时显示（Awareness 协议）
- [ ] 某用户编辑后，其他用户在 1 秒内看到更新（实时同步）
- [ ] 用户断线重连后，Yjs 自动同步缺失的 Update，无内容丢失
- [ ] CRDT 保证所有客户端最终一致，无论编辑顺序

###### Gateway

- [ ] Gateway 正确处理 100 个并发连接
- [ ] 服务端 Y.Doc 在断电后能恢复（持久化有效）
- [ ] Markdown 正确同步到 WizNote（最终一致性）

###### 降级保护

- [ ] WebSocket 断线时，自动切换到 HTTP 轮询模式（Phase 2 的降级）
- [ ] Gateway 不可用时，客户端本地 CRDT 继续工作，不阻塞编辑

---

## 八、附录

### A. 相关文件索引

| 文件路径 | 作用 |
|----------|------|
| `src/services/SyncService.js` | 备份/恢复/push/pull 核心逻辑 |
| `src/services/CloudSyncService.js` | UI 层同步服务 |
| `src-electron/main-process/electron-main.js` | SQLite IPC handlers，数据库操作 |
| `src/utils/DatabaseClient.js` | 渲染进程数据库客户端 |
| `src/store/server/notePersistenceService.js` | 笔记持久化操作（本地草稿创建、离线笔记升级） |
| `src/components/ui/dialog/OfflineSyncPromptDialog.vue` | 离线同步提示弹框 |
| `src/i18n/zh-cn/other.js` | 同步相关 i18n 字符串 |
| `src/i18n/en-us/other.js` | 同步相关 i18n 字符串 |

### B. 推荐学习资源

- **Yjs 官方文档**：https://docs.yjs.dev （生产级 CRDT，Notion 底层）
- **Automerge 2**：https://automerge.org （另一主流 CRDT 库）
- **Operational Transformation**：经典协同编辑算法，适合理解 Google Docs 原理
- **Syncthing**：开源文件同步工具，可参考其协议设计
- **Dotted Version Vectors**：适合数据库层面的版本追踪

### C. 类似项目参考

| 项目 | 技术栈 | 冲突处理方式 |
|------|--------|------------|
| Notion | Yjs CRDT | 无冲突（CRDT 自动合并） |
| Obsidian | Local-first + 可选插件 | 无实时协同，靠 Git sync |
| Logseq | Logseq 自身 CRDT | 类似 Yjs |
| Roam Research | Clojure Datascript | 操作日志 + 时间戳 |
| Dropbox Paper | OT + 自建协同服务 | 冲突时显示两个版本 |
| Syncthing | Delta sync + 自研协议 | 文件级冲突时保留双方副本 |

### D. Memocast sync_log 完整字段

```sql
CREATE TABLE IF NOT EXISTS sync_log (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  note_id INTEGER,
  action TEXT NOT NULL CHECK(action IN ('create', 'update', 'delete')),
  direction TEXT NOT NULL CHECK(direction IN ('local_to_server', 'server_to_local')),
  doc_guid TEXT,
  kb_guid TEXT,
  timestamp INTEGER,
  synced INTEGER DEFAULT 0,
  FOREIGN KEY (note_id) REFERENCES notes(id) ON DELETE CASCADE
);
```

### E. notes 表完整定义

```sql
CREATE TABLE IF NOT EXISTS notes (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  doc_guid TEXT,
  kb_guid TEXT,
  title TEXT NOT NULL DEFAULT 'Untitled',
  content TEXT DEFAULT '',
  category TEXT DEFAULT '/',
  tags TEXT DEFAULT '',
  data_created INTEGER,
  data_modified INTEGER,
  local_modified INTEGER,
  server_modified INTEGER,
  dirty INTEGER DEFAULT 0,
  created_at INTEGER,
  updated_at INTEGER
);
```
