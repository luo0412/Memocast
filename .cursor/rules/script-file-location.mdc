# 脚本存放规范：`_temp/` / `scripts/`

写完测试/验证脚本后，按用途放到对应文件夹：

| 用途 | 文件夹 | 说明 |
|-----|-------|-----|
| **一次性临时脚本**（调试、smoke test） | `_temp/` | 跑完即弃，不保留 |
| **可复用工具脚本**（CI、验证、构建） | `scripts/` | 保留，供后续重复使用 |

---

## 规范

1. **禁止**把脚本直接放项目根目录
2. **`_temp/`**：放调试用的临时脚本，命名自由（如 `scratch_xxx.mjs`、`_debug_xxx.js`）
3. **`scripts/`**：放正式工具脚本，命名规范（如 `verify-xxx.js`、`build-xxx.js`）
