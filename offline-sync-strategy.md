# coolma 离线同步策略说明

## 1. 设计目标

coolma 的同步设计不是把云端当作“协同编辑源”，而是尽量模拟本地文件夹与本地文件系统的使用习惯。

核心目标只有两个：

1. **目录语义与本地文件夹一致**
   - 同一路径下不允许出现重名笔记。
   - 创建、复制、移动、重命名时，都必须遵守这个约束。
   - 用户看到的行为，应尽量接近“在本地文件夹里管理 Markdown 文件”。

2. **本地永远优先，云端只作为补回与备份介质**
   - 本地有的内容永远优先于云端。
   - 云端有但本地没有的内容，可以同步回本地。
   - 一旦内容进入本地体系，之后就是本地覆盖云端，而不是双向协商。
   - 理论上我们不接受“云端编辑产生冲突”这种模型，云端不是主编辑面，只是备份库。

---

## 2. 总体原则

### 2.1 唯一性原则：同路径下禁止重名

这条原则是整个笔记管理体验的第一优先级。

等价表达：

- 一个目录就是一个本地文件夹。
- 一个笔记就是该目录下的一个文件。
- 因此同一路径下不能有两个同名笔记。

这个原则应覆盖以下所有入口：

- 新建笔记
- 导入笔记
- 复制笔记
- 移动笔记
- 重命名笔记
- 离线创建后再同步到云端
- 云端回补到本地

如果目标目录下已经有同名项，系统不能默默制造两个并存实体。

应对策略分两类：

- **创建 / 复制**：自动生成唯一名称，例如 `foo.md`、`foo (1).md`、`foo (2).md`
- **移动 / 重命名**：先检查目标路径是否冲突；如冲突则阻止操作或提示用户处理

这和本地文件系统的直觉保持一致。

---

### 2.2 本地优先原则：Local is source of truth

本地 SQLite 是系统内的真实数据源，云端不是。

可以把当前模型理解为：

- 本地：主库、工作副本、最终写入点
- 云端：远程备份库、补回来源、灾备副本

因此规则是：

- 本地存在的笔记，不被云端覆盖。
- 本地修改后，最终同步方向是“本地 -> 云端”。
- 云端回补只发生在“本地没有”时。
- 一旦某条笔记已经进入本地，后续即使云端也有版本，仍以本地为准。

这意味着我们不是传统意义上的双向同步，也不是 merge-based sync，而是：

> **本地单主写，云端单向承接。**

---

## 3. 当前实现如何体现这些原则

### 3.1 重名检查的底层语义

当前唯一性检查以“规范化后的 category + 规范化后的 title”作为唯一键。

具体表现：

- category 会做匹配规范化
- title 会 trim，并补默认值
- 最终通过 `category::title` 形式构造唯一键做比较

这意味着系统判断“是否同名”时，不是简单比字符串，而是按目录语义和标题语义判断。

### 3.2 创建时自动补唯一名称

对于创建 / 复制类操作，当前策略已经支持自动生成不冲突标题。

典型形式：

- `foo.md`
- `foo (1).md`
- `foo (2).md`

这非常符合本地文件系统习惯，建议继续坚持，不要引入随机后缀或不可读编码。

如果你说的“auto 加编码”是指自动附加可读序号，那么当前方向是对的；
如果未来要换成别的编码方式，也建议保持**可读、稳定、可预测**，否则会损伤本地文件感。

### 3.3 rename / move 也必须遵守同一路径唯一性

移动和重命名不能被视为“只是改 metadata”。

从文件系统视角看：

- rename = 在同目录下换文件名
- move = 把文件放到另一个目录
- rename + move = 改路径

所以它们都必须落到同一个原则上：

> **目标路径不能与现有笔记路径冲突。**

因此这两类操作都应在提交前执行目标路径检查。

当前代码已经沿这个方向处理：

- 重命名前检查目标 category + title
- 移动前检查目标 category + title
- 如果冲突则直接拦截

这是正确的，也必须继续保持。

---

## 4. 同步策略的本质

### 4.1 不是“双向合并”，而是“本地吸收 + 本地覆盖”

很多同步系统的默认假设是：

- 本地改一点
- 云端改一点
- 两边都可能是权威
- 最后需要比较时间戳、版本号、冲突合并

但 coolma 的模型不是这样。

coolma 的模型是：

1. 本地是唯一真实编辑面
2. 云端仅用于：
   - 恢复本地缺失数据
   - 接收本地备份
3. 不鼓励、也不依赖云端直接编辑
4. 同步内核采用双轨模型：
   - `dirty note` 负责新增与编辑
   - `delete log` 负责删除
5. 因此不把“线上编辑冲突”当作一个需要重点支持的常态场景

换句话说：

- 云端新增内容，本地可以拉下来
- 本地已有内容，云端不能反向压过来
- 本地修改之后，永远以本地版本覆盖云端

这就是你说的：

> 线上当备份库而已。

这个定位非常清晰，而且能极大降低同步复杂度。

---

### 4.2 云端拉取策略

云端拉取不是“全面覆盖同步”，而是“补缺”。

具体原则：

1. 获取云端笔记列表
2. 对每条云端笔记，先看本地是否已存在对应实体
3. 如果本地已存在，则跳过，不覆盖本地
4. 如果本地不存在，但本地已有“同路径 + 同标题”笔记，也跳过
5. 只有当本地确实没有时，才把云端笔记下载到本地

因此 pull 的职责是：

- 补回新设备未下载过的数据
- 从云端恢复本地尚不存在的笔记
- 不破坏本地已有工作集

这保证了“恢复”与“覆盖”严格区分。

---

### 4.3 本地上传策略

本地上传是纯本地优先策略的核心。

当前实现不是依赖一套泛化 `sync_log` 去记录所有 create / update / delete，而是拆成两条明确通道：

- `dirty note`：表示本地笔记需要推送到云端
- `delete log`：表示某条云端笔记需要被远端删除

#### 新增 / 编辑通道：dirty note

基本规则：

1. 所有创建、编辑、移动、重命名等写操作，先写入本地 SQLite
2. 只要笔记内容或路径有变更，就把该 note 标记为 `dirty = 1`
3. 上传时先读取本地最新版本
4. 如果该笔记已有云端 GUID，则直接更新云端
5. 如果没有云端 GUID，则按“同目录同标题”去云端查找是否已有对应项
6. 找到则更新那条云端记录
7. 找不到则在云端创建新笔记
8. 同步成功后，把云端 GUID 回写到本地，并清除 dirty 状态

这里最关键的是：

- 不做时间戳对比来决定谁覆盖谁
- 不做 merge
- 不做“如果云端更新更晚则采用云端”
- 而是明确执行：**本地内容覆盖云端内容**

这就是“本地单主写”的直接体现。

#### 删除通道：delete log

删除不再走“直接请求云端删除 + 本地顺带删掉”的即时模型，而是也纳入统一同步框架。

基本规则：

1. 业务层判断该笔记是否已具备真实云端身份（例如存在非 `local_` 前缀的 `doc_guid`）
2. 如果是云端已存在笔记，则先写一条自包含的 `delete log`
3. 该日志至少包含：
   - `note_id`
   - `doc_guid`
   - `kb_guid`
   - `timestamp`
4. 写完删除日志后，再删除本地 note
5. 后续由同步器统一读取待删除日志，调用云端删除 API
6. 删除成功后，将该日志标记为已同步并清理

这样做的意义是：

- 删除与新增/编辑一样，都是“先落本地状态，再由同步器统一推送”
- 删除日志本身是最小完备同步单元，不依赖本地 note 仍然存在
- 同步失败时可以重试，不会因为本地记录已删而丢失删除意图

因此，当前上传模型可以概括为：

> **dirty note 负责 push content，delete log 负责 push deletion。**

---

## 5. 为什么我们理论上不应该出现冲突

在当前设计中，冲突不是“功能”，而是应该被架构消灭的事情。

### 5.1 冲突在何种模型下才会成为常态

冲突通常来自以下前提：

- 多端都可以直接编辑
- 云端也是实时真源
- 本地与云端都可能独立变更
- 最终系统需要合并两个都合法的版本

但我们当前并不认同这个模型。

### 5.2 coolma 的前提不同

coolma 的默认前提是：

- 用户主要在本地客户端编辑
- 本地 SQLite 保留最终版本
- 云端只负责备份、补回和托管
- 云端侧不应成为并行编辑源

所以在理论上：

- 云端修改不应该成为一条需要被尊重的独立编辑线
- 本地只要已有内容，就没有必要让云端版本反向覆盖
- 因此真正需要解决的不是“冲突合并”，而是“如何避免把云端当第二主写端”

这也是为什么我们说：

> 我们理论上不允许线上的修改编辑产生冲突。

更准确地说，是：

> 我们不把云端并行编辑视作产品支持目标，因此同步层不以冲突合并为核心职责。

---

## 6. 这套策略对功能设计的直接约束

### 6.1 所有写操作先落本地

任何编辑动作都应先写入本地 SQLite，再异步或后续同步到云端。

不能反过来把云端当写入口再回写本地，否则会破坏本地优先原则。

### 6.2 所有命名相关操作都要以“目标路径唯一”收口

不要把“创建、复制、重命名、移动”分裂成四套规则。

从模型上，它们都在做一件事：

- 试图占用一个新的逻辑路径

因此统一规则应是：

- 目标路径不存在：允许
- 目标路径已存在：
  - 创建/复制 → 自动生成新名字
  - 移动/重命名 → 阻止或提示

### 6.3 云端回补只能新增，不能改写本地现有内容

即使云端有内容变化，只要本地已经有该笔记，原则上就不应被云端回灌覆盖。

允许的例外应该非常少，而且要有明确语义，例如：

- 仅对“本地空壳记录”做一次内容回填
- 或者用户显式执行“从云端恢复缺失数据”

但这些都必须是受控例外，而不是默认同步行为。

### 6.4 删除也必须先落本地，再由同步器统一推送

删除场景不应成为同步体系里的特例。

当前实现要求：

- 业务层先决定是否需要记录删除意图
- 如果该笔记对应真实云端笔记，则先写 `delete log`
- 然后立即删除本地 note，保证本地体验及时完成
- 后续由同步器统一处理云端删除与重试
- 删除成功后清理已完成的 delete log

这条规则和“所有编辑先落本地”本质一致，只不过删除通过日志表达，而不是通过 dirty note 表达。

### 6.5 云端错误只作为兜底，不改变本地策略

如果云端返回同名、权限、目录不存在等错误，前端应该：

- 提示用户失败原因
- 保留本地状态
- 等待用户下次重试或进一步处理

不能因为云端失败，就回退本地设计原则。

---

## 7. 推荐的产品表述

如果要把这套原则写成面对团队或未来维护者都易懂的版本，可以浓缩为下面几句：

### 7.1 一句话版本

**coolma 是本地优先的笔记系统，云端是备份库，不是协同主库。**

### 7.2 命名规则版本

**笔记路径语义等同于本地文件路径；同一路径下不允许重名。**

### 7.3 同步规则版本

**云端只补回本地没有的数据；本地一旦存在，即永远以本地覆盖云端；新增/编辑走 dirty，删除走 delete log。**

### 7.4 冲突观点版本

**我们不以双向冲突合并为目标，而是通过“本地单主写”原则，从设计上减少冲突面。**

---

## 8. 结论

coolma 的离线同步策略，本质上不是传统云笔记的“双向同步”，而是：

1. 用本地文件夹习惯定义笔记组织方式
2. 用“同路径不可重名”定义笔记唯一性
3. 用本地 SQLite 作为唯一真实编辑源
4. 用云端承担补缺、恢复与备份职责
5. 用“本地覆盖云端”代替“本地云端双向合并”
6. 用 `dirty note + delete log` 双轨模型收口同步实现

因此，后续所有实现都应该围绕以下核心原则收口：

- **路径唯一性优先**
- **本地优先于云端**
- **云端新增可补回，本地既有不可被覆盖**
- **不把云端并行编辑当成主场景**

这四条如果不变，整个同步体系就会保持一致、可解释，也更接近用户对“本地文件系统”的直觉。
