# 存储机制切换方案

> 日期：2026-06-21
> 项目：Memocast - 笔记应用存储机制双轨制

---

## 背景与目标

目前 Memocast 所有笔记数据存储在 `memocast.db`（sql.js SQLite）中，笔记内容以 Markdown 文本形式存在 `notes.content` 字段。本方案为笔记引入第二种存储机制：**本地 MD 文件**（即 `.md` 文件直接写入磁盘），支持用户在两种机制之间自由切换，并提供完整的数据备份与转换逻辑。

---

## 架构设计

### 两种存储机制

| 维度 | SQLite（当前） | 本地 MD 文件（新增） |
|------|-------------|------------------|
| 存储位置 | `{userData}/memocast.db` | `{userData}/notes/` 目录树 |
| 内容格式 | `content` 字段存 Markdown 文本 | 每篇笔记一个 `.md` 文件 |
| 分类映射 | `category` 字段（路径字符串） | 对应 `{category}/` 子目录结构 |
| 标签 | `note_tags` / `tags` 表 | Front-matter `tags:` 字段 |
| 元数据 | `data_created`, `data_modified` 等列 | Front-matter YAML 头 |
| 索引/搜索 | SQLite 全文索引 | 文件系统 + 内存索引 |
| 同步支持 | 现有 dirty 机制完全兼容 | 同左（MD 内容即同步内容） |

### 核心设计原则

1. **本地优先不变**：两种机制均为本地优先，云端（WizNote）仅作备份。
2. **数据模型统一**：数据库中仍保留 `notes` 表作为元数据索引和同步状态载体，只是 `content` 字段在 MD 模式下可为空或存文件路径引用。
3. **静默切换**：切换存储机制后，新笔记自动使用新机制；旧笔记按需迁移或保持原机制存储。
4. **可逆切换**：用户可随时切回，迁移是增量且可中断的。

### 目录结构（MD 模式）

```
{userData}/
└── notes/
    └── {category}/
        └── {title}.md
```

示例：

```
memocast/
├── notes/
│   ├── /                      ← 根目录
│   │   └── Untitled.md
│   ├── /工作/
│   │   ├── 项目A.md
│   │   └── 项目B.md
│   └── /工作/子目录/
│       └── 文档.md
```

---

## 存储机制配置

### 配置键值

通过 `app_state` 表存储当前机制：

```
key: "storage.mechanism"
value: "sqlite" | "md-files"
```

### 迁移状态跟踪

```
key: "storage.migration"
value: {
  from: "sqlite" | "md-files",
  to: "sqlite" | "md-files",
  phase: "idle" | "exporting" | "importing" | "completed" | "failed",
  progress: { current: number, total: number },
  lastError: string | null
}
```

---

## 数据模型变更

### notes 表新增字段

```sql
ALTER TABLE notes ADD COLUMN content_file TEXT;
ALTER TABLE notes ADD COLUMN storage_mechanism TEXT DEFAULT 'sqlite';
```

- `content_file`：MD 模式下指向文件路径，如 `/工作/项目A.md`
- `storage_mechanism`：标识该笔记所属存储机制（切换后新笔记用新机制，旧笔记保持原值）

### Front-matter 结构（MD 文件头）

每个 MD 文件头部包含 YAML front-matter：

```yaml
---
doc_guid: local_1234567890_abc123
kb_guid: ""
title: 项目A
category: /工作/
tags: [tag1, tag2]
data_created: 1717654321000
data_modified: 1717654321000
local_modified: 1717654321000
created_at: 1717654321000
updated_at: 1717654321000
dirty: 1
---
```

---

## 实现步骤

### 阶段 1：基础设施层

#### 1.1 新增存储机制配置与服务

**文件：** `src/services/StorageService.js`（新建）

职责：
- 统一管理存储机制的读取/写入接口
- 根据当前机制路由到对应存储适配器
- 暴露 `getMechanism()` / `setMechanism(mechanism)` 接口

```javascript
class StorageService {
  async getMechanism()     // 读取 app_state['storage.mechanism']，默认 'sqlite'
  async setMechanism(m)    // 写入配置（切换前先做备份检查）

  // 统一读写接口
  async saveNote(note)     // 根据当前机制路由
  async loadNote(docGuid)  // 根据当前机制路由
  async deleteNote(docGuid)
  async listNotes(options) // 获取笔记列表
  async exportAll()        // 导出所有笔记
  async importAll(files)   // 从 MD 文件批量导入
}
```

#### 1.2 新增 MD 文件存储适配器

**文件：** `src/lib/storage/MdFileAdapter.js`（新建）

职责：
- 负责 MD 文件的读写、目录创建、front-matter 解析/序列化
- `noteToFile(note)` → 将笔记写入 `{category}/{title}.md`
- `fileToNote(filePath)` → 解析 MD 文件为笔记对象
- `deleteFile(category, title)` → 删除文件
- `listFiles()` → 遍历 `notes/` 目录获取所有 MD 文件

Front-matter 解析：使用 `js-yaml` 或手动解析 `---` 分隔的 YAML 头。

路径安全处理：
- 将 `category` 路径转为文件系统路径（`/` → 子目录名）
- 将 `title` 作为文件名（处理 `/ \ : * ? " < > |` 等非法字符）

#### 1.3 新增 SQLite 存储适配器

**文件：** `src/lib/storage/SqliteAdapter.js`（新建）

职责：
- 将现有 `DatabaseClient.notes` 操作封装为适配器接口
- 保持与 MdFileAdapter 统一的 API

```javascript
class SqliteAdapter {
  async saveNote(note)   // 等同现有 create/update 逻辑
  async loadNote(docGuid)
  async deleteNote(docGuid)
  async listNotes(options)
}
```

---

### 阶段 2：迁移引擎

#### 2.1 导出（SQLite → MD 文件）

**文件：** `src/lib/storage/MigrationEngine.js`（新建）

```javascript
async exportToMdFiles(options = {}) {
  // 1. 获取所有 SQLite 笔记（分批，每批 50 条）
  // 2. 遍历每条笔记：
  //    a. 序列化 front-matter + content → MD 文件
  //    b. 写入 {userData}/notes/{category}/{title}.md
  //    c. 更新 notes.content_file 字段
  //    d. 标记 notes.storage_mechanism = 'md-files'
  // 3. 保存数据库
  // 4. 返回 { exported: N, failed: M, errors: [] }
}
```

#### 2.2 导入（MD 文件 → SQLite）

```javascript
async importFromMdFiles(options = {}) {
  // 1. 遍历 {userData}/notes/ 目录树
  // 2. 解析每个 .md 文件的 front-matter
  // 3. 读取文件内容作为 content
  // 4. 调用 DatabaseClient.notes.create / update
  // 5. 更新 notes.content_file 字段
  // 6. 标记 notes.storage_mechanism = 'sqlite'
}
```

#### 2.3 备份逻辑

切换前自动执行备份：

1. **SQLite → MD 切换前**：将整个 `memocast.db` 复制为 `memocast.db.backup.{timestamp}`
2. **MD → SQLite 切换前**：将整个 `notes/` 目录复制为 `notes.backup.{timestamp}/`
3. 备份文件保留最近 3 份

#### 2.4 迁移进度跟踪

- 通过 `app_state['storage.migration']` 记录进度
- 支持中断续传：已迁移的笔记跳过
- 失败记录到 `errors[]` 数组，可手动重试

---

### 阶段 3：UI 层

#### 3.1 设置页面入口

**文件：** `src/components/ui/dialog/StorageSettingsDialog.vue`（新建）

- 单选切换：`SQLite 数据库` vs `本地 MD 文件`
- 显示当前机制、笔记总数、数据库大小/磁盘占用
- 切换按钮：显示警告对话框（"切换存储机制将触发数据迁移，可能需要几分钟...")
- 迁移进度条（实时刷新 progress）
- 迁移完成后显示摘要（成功 N 条，失败 M 条）

#### 3.2 设置页面入口位置

在现有设置对话框中增加"存储"Tab 或直接在通用设置中增加选项。

---

### 阶段 4：集成与切换

#### 4.1 笔记读写路由

修改 `src/store/server/notePersistenceService.js` 中的 `upsertLocalNoteByDocGuid`：

```javascript
// 根据当前存储机制选择存储路径
const mechanism = await StorageService.getMechanism()
if (mechanism === 'md-files') {
  await MdFileAdapter.saveNote(note)
} else {
  await SqliteAdapter.saveNote(note)
}
```

#### 4.2 笔记列表加载

修改 `src/store/server/localDataService.js` 中的笔记加载逻辑，按存储机制过滤并合并结果：

```javascript
// SQLite 笔记
const sqliteNotes = await DatabaseClient.notes.getAll(options)

// MD 文件笔记（storage_mechanism = 'md-files' 且 content_file 不为空）
const mdNotes = await MdFileAdapter.listNotes(options)

// 合并、去重（按 doc_guid）
return deduplicateByDocGuid([...sqliteNotes, ...mdNotes])
```

#### 4.3 笔记内容加载

修改 `src/store/server/actions.js` 中 `getNoteContent`：

```javascript
const localNote = await DatabaseClient.notes.getByDocGuidWithPriority(docGuid)

if (localNote?.storage_mechanism === 'md-files' && localNote?.content_file) {
  // 从 MD 文件读取内容
  const content = await MdFileAdapter.readNoteContent(localNote.content_file)
  result.html = content
} else if (localNote && localNote.id) {
  // 从 SQLite 读取
  result.html = localNote.content
} else {
  // 回退到云端
  ...
}
```

#### 4.4 删除笔记

```javascript
if (mechanism === 'md-files') {
  await MdFileAdapter.deleteNote(docGuid)
}
// SQLite 中仍保留元数据记录（标记为 deleted 或直接删除）
await DatabaseClient.notes.remove(id)
```

---

## 文件变更清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `src/services/StorageService.js` | 新建 | 存储机制统一管理器 |
| `src/lib/storage/MdFileAdapter.js` | 新建 | MD 文件存储适配器 |
| `src/lib/storage/SqliteAdapter.js` | 新建 | SQLite 存储适配器（封装现有逻辑） |
| `src/lib/storage/MigrationEngine.js` | 新建 | 迁移引擎（导出/导入/备份） |
| `src-electron/main-process/electron-main.js` | 修改 | 新增 notes 表字段迁移；新增 IPC 处理器 |
| `src/utils/DatabaseClient.js` | 修改 | 新增 `notes.setMechanism` / `notes.getMechanism` |
| `src/store/server/notePersistenceService.js` | 修改 | 路由到对应存储适配器 |
| `src/store/server/actions.js` | 修改 | 加载笔记时按机制读取内容 |
| `src/store/server/localDataService.js` | 修改 | 合并多存储机制笔记列表 |
| `src/components/ui/dialog/StorageSettingsDialog.vue` | 新建 | 存储设置 UI |

---

## 风险与注意事项

1. **文件名冲突**：同目录下标题相同的笔记需要去重，可加序号后缀（`项目A.md`, `项目A (1).md`）
2. **分类路径安全**：将 `/` 转为合法目录名，如 `工作` → `_root_工作` 或直接用 `_` 表示根目录
3. **内容编码**：MD 文件统一使用 UTF-8，解析时需处理 BOM
4. **迁移原子性**：备份 → 迁移 → 验证 → 切换，分步执行，失败可回滚
5. **性能**：MD 文件模式下，笔记列表加载需遍历文件系统，建议内存缓存
6. **WizNote 同步**：同步层不感知存储机制变更，仍按现有 dirty 机制工作

---

## 测试计划

1. **存储机制读取**：切换后重新打开应用，验证机制配置正确读取
2. **新建笔记**：在两种机制下分别创建笔记，验证写入正确
3. **读取笔记**：在两种机制下分别打开笔记，验证内容一致
4. **删除笔记**：验证文件和数据库记录同时清理
5. **迁移导出**：从 SQLite 导出到 MD 文件，验证所有字段正确序列化
6. **迁移导入**：从 MD 文件导入到 SQLite，验证 front-matter 正确解析
7. **双向切换**：SQLite → MD → SQLite 循环切换，验证数据不丢失
8. **同步兼容性**：切换后执行备份到云端，验证 dirty 机制正常工作
