# Memocast 优化参考计划 —— 借鉴 Yank Note (YN)

> 基于 [Yank Note (purocean/yn)](https://github.com/purocean/yn) 的特性，对 Memocast 进行改进的方向梳理。

---

## 一、编辑器增强

### 1.1 AI 助手集成（已有雏形，可深化）

**YN 提供：**
- AI Copilot：文本补全、生成、图片生成（兼容 OpenAI、Ollama、Gemini、Kimi、Flux.1 等）
- OpenCode AI Agent：智能编程辅助
- AI 对话式交互

**Memocast 现状：**
- 已有 `PortkeyService` + Element-UI-X 聊天抽屉（AI Demo）
- 仅支持流式对话，无文本补全、代码生成、图片生成能力

**可借鉴方向：**
- 引入 AI 文本补全（inline copilot），在编辑器内光标位置直接给出补全建议
- 支持图片生成：选中 Markdown 图片语法描述 → AI 生成图片
- 支持更多模型：兼容 Ollama（本地部署）、Kimi 等国产模型
- AI 对话结果一键插入笔记

---

### 1.2 版本控制（全新功能）

**YN 提供：**
- 内置笔记历史版本回溯（每次保存生成快照）

**Memocast 现状：**
- 无笔记版本历史机制
- 依赖 WizNote 云的变更记录（且仅在 dirty 字段层面）

**可借鉴方向：**
- 基于本地 SQLite 实现轻量版本控制：每次同步成功 / 定期快照
- 版本历史界面：查看、回退、对比差异
- 与现有 SyncService 融合，不干扰 dirty 状态逻辑

---

### 1.3 文档加密（全新功能）

**YN 提供：**
- `.c.md` 后缀文件视为加密文件，密码单独设置，前端完成加解密

**Memocast 现状：**
- `crypto-js` 已在 devDependencies 中，但未在笔记层面实现加密
- WizNote API 层面无加密机制

**可借鉴方向：**
- 笔记加密开关，设置独立密码
- 加密后本地 SQLite 中存储密文，云端同步密文
- 解密后内存中编辑，关闭时重新加密
- 可选：`crypto-js` AES-256 实现

---

### 1.4 代码块增强（已有 Monaco，可扩展）

**YN 提供：**
- 可运行代码块（JS、PHP、Node.js、Python、Bash）
- 代码块内置终端输出结果

**Memocast 现状：**
- 已有 Monaco 编辑器用于 Rune 模板编辑
- 笔记内代码块仅展示，无运行能力

**可借鉴方向：**
- 代码块结果预览（eval in sandbox 或调用本地解释器）
- 代码块一键复制 + 运行按钮
- 支持更多语言的语法高亮（shiki 已在依赖中）

---

### 1.5 宏替换 / 动态内容（全新功能）

**YN 提供：**
- 支持在 Markdown 中嵌入 JavaScript 表达式，渲染时动态求值
- 示例：嵌入计算公式、时间戳、随机内容等

**Memocast 现状：**
- 无类似机制

**可借鉴方向：**
- 引入动态变量语法（如 `{{$now}}`、`{{$random()}}`）
- 保存时将动态内容渲染为静态文本，或标记 dirty 以便后续同步

---

## 二、导出与发布

### 2.1 多格式导出（已有 Markdown，可扩展）

**YN 提供：**
- Pandoc 后端支持多种格式导出（PDF、HTML、DOCX 等）
- 内置导出面板

**Memocast 现状：**
- 支持导出为本地 Markdown 文件
- 无 PDF、HTML 等富格式导出

**可借鉴方向：**
- 引入 `electron-pdf` / `puppeteer` 或 Pandoc 实现 PDF 导出
- HTML 单文件导出（图片内联 Base64）
- DOCX 导出（通过 `docx` npm 包）
- 博客平台专属导出模板（Hexo、Hugo、Jekyll）

---

### 2.2 博客自动部署（已有 BlogDeployService，可深化）

**YN 提供：**
- 内置博客发布流程

**Memocast 现状：**
- 已有 `BlogDeployService.js` + `BlogDeployDialog.vue`（GitHub Pages 部署）
- 参见 `TODO-博客自动打包部署-202609.md`

**可借鉴方向：**
- 支持更多平台：Vercel、Netlify、自定义 CI 脚本
- 部署前预览（本地 HTTP 服务器）
- 一键发布对话框集成到笔记工具栏

---

### 2.3 外部链接转换（全新功能）

**YN 提供：**
- 自动将外部图片链接下载并转为本地图片
- 外部链接失效检测

**Memocast 现状：**
- 笔记内图片链接需手动维护
- WizNote 资源上传已支持，但无自动下载转换

**可借鉴方向：**
- 笔记保存时检测外部图片，自动下载并上传到 WizNote 资源库
- 链接有效性检查工具

---

## 三、嵌入与渲染

### 3.1 图表嵌入（部分已有，可扩展）

**YN 提供：**
- PlantUML、Draw.io、ECharts、Mermaid、Luckysheet 表格内嵌渲染

**Memocast 现状：**
- `mermaid`、`echarts` 已在依赖中
- Muya 编辑器支持部分嵌入块

**可借鉴方向：**
- 完善 PlantUML / Draw.io 嵌入渲染（`@vscode/drawio` 集成思路）
- Luckysheet 多人协作表格嵌入
- 图表编辑器 UI：双击图表块打开可视化编辑器

---

### 3.2 思维导图（已有 MarkMap，可深化）

**Memocast 现状：**
- 已有 `MarkMapDialog.vue`
- 以对话框形式存在，非内嵌

**可借鉴方向：**
- 将 MarkMap 内嵌到笔记内容中（类似 Echo 渲染块）
- 支持从笔记的大纲结构一键生成思维导图
- 思维导图编辑态直接在笔记内渲染

---

### 3.3 PPT / 幻灯片（已有 Reveal.js，可深化）

**Memocast 现状：**
- 已有 `PptPreviewDialog.vue`（Reveal.js）

**可借鉴方向：**
- 将 Markdown 标记（`---` 分页）实时渲染为幻灯片预览
- 幻灯片导出为 PDF
- 与 MarkMap 联动：幻灯片内嵌入思维导图

---

### 3.4 HTML Applets（全新功能）

**YN 提供：**
- 支持在笔记中嵌入 HTML 小程序（沙箱运行）

**Memocast 现状：**
- 无类似机制

**可借鉴方向：**
- 引入 iframe 沙箱嵌入自定义 HTML 组件
- 可作为 Echo 系统的扩展：Echo 支持渲染自定义 HTML 小程序

---

### 3.5 脚注支持（全新功能）

**YN 提供：**
- 标准 Markdown 脚注语法

**Memocast 现状：**
- 无脚注渲染支持（markdown-it 插件）

**可借鉴方向：**
- 引入 `markdown-it-footnote` 插件
- 脚注渲染样式适配主题

---

### 3.6 自定义容器（全新功能）

**YN 提供：**
- 类似 VuePress 的 `::: tip`、`::: warning` 自定义提示块

**Memocast 现状：**
- 无自定义容器渲染

**可借鉴方向：**
- 引入 `markdown-it-container` 插件
- 支持 tip/warning/danger/info 四种容器样式
- 适配深浅主题配色

---

## 四、写作体验优化

### 4.1 双向链接与文档图谱（全新功能）

**YN 提供：**
- 文档链接：笔记间互相跳转

**Memocast 现状：**
- 笔记间跳转需通过链接手动实现
- 无反向链接收集

**可借鉴方向：**
- 实现笔记间的双向链接（`[[笔记名]]` 语法）
- 自动收集反向链接（"哪些笔记引用了本文"）
- 笔记图谱可视化（类似 Obsidian）

---

### 4.2 快速打开与全局搜索（已有搜索，可深化）

**YN 提供：**
- 快捷键呼出文件切换面板
- 全文搜索、标签搜索、文件名搜索

**Memocast 现状：**
- 已有 `SearchDialog.vue`
- 基于 SQLite FTS（全文搜索引擎）

**可借鉴方向：**
- 全局快捷键绑定（如 `Cmd/Ctrl+P`）
- 搜索结果高亮当前匹配词
- 模糊搜索（fuse.js）
- 最近编辑 / 最近访问笔记快速访问

---

### 4.3 同步滚动优化（全新功能）

**YN 提供：**
- 编辑区与预览区同步滚动
- 预览区可独立滚动

**Memocast 现状：**
- 当前为 WYSIWYG 编辑（Muya），所见即所得，无需预览区同步

**可借鉴方向：**
- 如果未来引入分离编辑/预览模式，需实现同步滚动
- 可参考 Muya 的 scroll emitter 机制

---

### 4.4 剪贴板增强（部分已有）

**YN 提供：**
- 剪贴板图片一键粘贴为文件或 Base64
- 粘贴时自定义文件名模板（时间戳、哈希）

**Memocast 现状：**
- 支持粘贴图片，但文件名处理较简单

**可借鉴方向：**
- 文件名模板：`{date}-{hash}.png` 格式
- 支持粘贴 HTML → 转为 Markdown（已有 `turndown`）
- 支持粘贴富文本保留格式

---

### 4.5 自动保存与状态提示（已有，可优化）

**Memocast 现状：**
- 有自动保存机制
- 标题栏橙色提醒未保存状态

**可借鉴方向：**
- 未保存时标题栏更明显的视觉提示（如红点、图标）
- 保存状态细分：本地保存成功 → 同步成功 → 云端确认
- 保存冲突提示 UI

---

## 五、插件系统

### 5.1 插件扩展机制（全新功能）

**YN 提供：**
- 用户编写 JavaScript 插件，放置在 `home directory/plugins`
- 插件可扩展编辑器功能、自定义工具栏、注册快捷键等

**Memocast 现状：**
- 无插件机制
- Echo/Rune 系统可视为内置扩展

**可借鉴方向：**
- 定义插件 API（注册菜单、工具栏按钮、快捷键、编辑器扩展点）
- 插件加载器（读取用户插件目录，动态执行）
- 插件市场 / 插件管理器 UI
- 内置 `electron-repl` 风格的开发者控制台

---

## 六、表格增强

### 6.1 表格编辑器优化

**YN 提供：**
- 双击单元格快速编辑
- 表格标题支持多行文本、列表
- 表格增强渲染

**Memocast 现状：**
- Muya 内置表格支持
- 无单元格快速编辑弹出层

**可借鉴方向：**
- 双击单元格弹出内联编辑器
- 列宽拖拽调整
- 表格工具栏：添加/删除行列、排序、公式

---

## 七、性能与工程

### 7.1 大文档性能优化

**YN 特点：**
- 基于 Monaco，内置虚拟滚动，大文档无卡顿

**Memocast 现状：**
- Muya 编辑器处理超长文档（>10万字）可能有性能问题

**可借鉴方向：**
- Monaco 作为可选编辑器的完善（当前仅用于 Rune）
- 大文档虚拟渲染测试与优化
- 笔记列表虚拟滚动（如果尚未实现）

---

### 7.2 依赖版本更新

**当前痛点：**
- Vue 2.7（长期维护到 2024 年底）
- Electron 41（较新）
- sql.js 1.14（WebAssembly SQLite）

**可优化：**
- Electron 新版本带来的性能、安全、API 改进
- Monaco 升级（当前 0.38，可升级到 0.52+）
- Shiki 升级以支持更多语言和主题

---

## 八、其他可借鉴项

| 功能 | YN 实现 | Memocast 现状 | 可行性 |
|------|---------|---------------|--------|
| 附件嵌入 | 支持文件附件点击打开 | 无 | 中 |
| 目录生成 | `[toc]` 语法自动生成目录 | 无（Muya 部分支持） | 高 |
| 深色主题细节 | GitHub 风格渲染 | 有 Quasar 主题 | 持续 |
| 多仓库支持 | 定义多个文档目录分类 | 现有单仓库模式 | 中 |
| MCP 工具 | 提供 MCP 设置检查工具 | 无 | 中 |
| 本地终端集成 | 内置终端面板 | 无 | 低 |

---

## 九、优先级建议

### 第一梯队（高价值、中低难度）

1. **AI 深化**：引入 inline copilot 补全 + 图片生成
2. **文档版本历史**：基于 SQLite 的快照机制
3. **快速搜索优化**：模糊搜索 + 全局快捷键
4. **双向链接**：`[[笔记]]` 语法 + 反向链接收集
5. **表格编辑器增强**：双击编辑、列宽拖拽

### 第二梯队（高价值、中高难度）

6. **插件系统**：用户插件 API + 插件管理器
7. **多格式导出**：PDF、HTML、DOCX
8. **版本控制**：增量快照与回退
9. **图表渲染完善**：PlantUML、Draw.io 内嵌编辑器
10. **博客部署扩展**：支持 Vercel、Netlify

### 第三梯队（长期愿景）

11. 笔记加密
12. 宏替换动态内容
13. HTML Applets
14. 多仓库支持
15. 代码块运行

---

## 十、参考资源

- YN 项目主页：https://github.com/purocean/yn
- YN 特性说明文档（需从项目 README 跳转）
- YN 特性截图展示
